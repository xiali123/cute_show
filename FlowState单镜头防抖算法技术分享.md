# FlowState 单镜头防抖算法 - 技术分享

> **分享目标**: 向团队介绍 FlowState 单镜头防抖算法原理与实现
> **受众**: 算法工程师、软件工程师、测试工程师
> **时长**: 约 60-90 分钟
> **文档版本**: 3.0 (详细版)
> **创建日期**: 2026-04-20

---

## 目录

1. [算法背景与价值](#1-算法背景与价值)
2. [数学基础知识](#2-数学基础知识)
3. [姿态估计算法深度解析](#3-姿态估计算法深度解析)
4. [滑动窗口平滑算法](#4-滑动窗口平滑算法)
5. [滚动快门校正原理](#5-滚动快门校正原理)
6. [FlowMap 生成与投影变换](#6-flowmap-生成与投影变换)
7. [性能优化策略](#7-性能优化策略)
8. [参数配置指南](#8-参数配置指南)
9. [常见问题与解决方案](#9-常见问题与解决方案)

---

## 1. 算法背景与价值

### 1.1 什么是视频防抖？

视频防抖的目标是消除相机运动带来的不必要抖动，同时保留有意运动。

```
原始视频:
      ╱╲╱╲╱╲╱╲    严重抖动
     ╱━━╱━━╱━━     难以观看

防抖后:
    ━━━━━━━━━      平滑稳定
```

### 1.2 为什么需要 IMU 防抖？

| 方案 | 优点 | 缺点 | 适用场景 |
|------|------|------|---------|
| **纯光学防抖** | 图像质量高 | 成本高、角度有限 | 单反/微单 |
| **纯电子防抖** | 成本低 | 裁剪大、暗光差 | 入门设备 |
| **纯视觉防抖** | 无需额外硬件 | 快速运动失败 | 固定机位 |
| **IMU+视觉融合** | 鲁棒性强、视野大 | 算法复杂 | **专业设备** |

**FlowState 选择 IMU+视觉融合的原因**：

1. **IMU 提供绝对姿态**：不受场景纹理影响
2. **高频响应**：200Hz 采样率捕捉快速运动
3. **低光鲁棒**：不依赖图像特征
4. **六自由度**：完整捕捉 3D 旋转

---

## 2. 数学基础知识

### 2.1 四元数数学基础

#### 定义

四元数是复数的推广，由一个实部和三个虚部组成：

```
q = w + xi + yj + zk
```

其中：
- `w` 是实部（标量部分）
- `x, y, z` 是虚部（向量部分）
- `i, j, k` 满足：i² = j² = k² = ijk = -1

**单位四元数**（表示旋转）满足：
```
|q| = √(w² + x² + y² + z²) = 1
```

#### 四元数与旋转的对应关系

对于单位四元数 `q = [w, (x, y, z)]`，它表示绕单位向量 `n = (nx, ny, nz)` 旋转角度 `θ`：

```
w = cos(θ/2)
x = nx · sin(θ/2)
y = ny · sin(θ/2)
z = nz · sin(θ/2)
```

**示例**：绕 Z 轴旋转 90°

```
θ = 90° = π/2
n = (0, 0, 1)

w = cos(π/4) = √2/2 ≈ 0.707
x = 0 · sin(π/4) = 0
y = 0 · sin(π/4) = 0
z = 1 · sin(π/4) = √2/2 ≈ 0.707

q = [0.707, 0, 0, 0.707]
```

#### 四元数乘法（旋转叠加）

两个四元数的乘积表示旋转的叠加：

```
q_result = q1 * q2
```

矩阵形式表示：

```
┌─────────────────┐   ┌─────────────────┐   ┌─────────────────┐
│ w_result        │   │ w1    -x1  -y1  -z1│   │      w2        │
│ x_result        │ = │ x1     w1  -z1   y1│ · │      x2        │
│ y_result        │   │ y1     z1   w1  -x1│   │      y2        │
│ z_result        │   │ z1    -y1   x1   w1│   │      z2        │
└─────────────────┘   └─────────────────┘   └─────────────────┘
```

展开形式：

```
w_result = w1·w2 - x1·x2 - y1·y2 - z1·z2
x_result = w1·x2 + x1·w2 + y1·z2 - z1·y2
y_result = w1·y2 - x1·z2 + y1·w2 + z1·x2
z_result = w1·z2 + x1·y2 - y1·x2 + z1·w2
```

#### 球面线性插值 (SLERP)

**问题**：如何在两个四元数之间平滑过渡？

线性插值的问题：
```
q_linear(t) = (1-t)·q0 + t·q1
```
结果不是单位四元数，需要归一化，且在球面上不是最短路径。

**SLERP 公式**：

```
q(t) = sin((1-t)θ)/sin(θ) · q0 + sin(tθ)/sin(θ) · q1
```

其中 `θ = arccos(q0 · q1)` 是两个四元数之间的角度。

**推导过程**：

1. 计算两个四元数的点积：
```
cos(θ) = q0 · q1 = w0·w1 + x0·x1 + y0·y1 + z0·z1
```

2. 如果点积为负，取反 q1 确保最短路径：
```
if cos(θ) < 0:
    q1 = -q1
    cos(θ) = -cos(θ)
```

3. 计算插值系数：
```
k0 = sin((1-t)θ) / sin(θ)
k1 = sin(tθ) / sin(θ)
```

4. 线性组合并归一化：
```
q(t) = normalize(k0 · q0 + k1 · q1)
```

**可视化**：

```
        q1
       ╱│╲
      ╱ │ ╲  球面上的大圆弧（最短路径）
     ╱  │  ╲
    ╱   │   ╲
   q0───┼───q(t)  t ∈ [0, 1]
        │
       t=0.5
```

### 2.2 IMU 测量模型与误差分析

#### 陀螺仪测量模型

```
ω_measured = ω_true + b_g + n_g + ε_g
```

各项含义：
- `ω_true`：真实角速度
- `b_g`：偏差（Bias，慢变，主要误差源）
- `n_g`：噪声（Noise，零均值高斯白噪声）
- `ε_g`：其他误差（温度漂移、交叉轴耦合等）

**偏差随时间漂移**：

```
b_g(t) = b_g(0) + ∫(η_bias · dt) + w_temp(T)
```

其中：
- `η_bias`：偏差随机游走
- `w_temp(T)`：温度相关漂移

** Allan 方差分析**：

```
Allan Variance vs Averaging Time:

σ²(τ) ∝ 
  - τ⁻²  : 量化噪声
  - τ⁻¹  : 角度随机游走
  - τ⁰   : 偏差不稳定性
  - τ¹   : 速率随机游走
```

#### 加速度计测量模型

```
f_measured = R^T · (a - g) + b_a + n_a + ε_a
```

各项含义：
- `R`：姿态旋转矩阵
- `a`：真实加速度（车辆运动+重力）
- `g`：重力向量 [0, 0, 9.81] m/s²
- `b_a`：加速度计偏差
- `n_a`：测量噪声
- `ε_a`：其他误差（交叉轴耦合、非线性等）

**应用场景分析**：

| 状态 | 加速度计测量 | 陀螺仪作用 | 加速度计作用 |
|------|-------------|-----------|-------------|
| 静止 | 仅重力 | 积分漂移 | **校正姿态** |
| 匀速 | 仅重力 | 积分漂移 | **校正姿态** |
| 加速 | 重力+运动 | **主导** | 辅助校正 |
| 快速运动 | 噪声大 | **主导** | 不可靠 |

---

## 3. 姿态估计算法深度解析

### 3.1 姿态估计问题定义

**目标**：根据 IMU 测量值，实时估计相机姿态 `q_i_w(t)`。

```
输入:
  - 陀螺仪: ω(t) = [ωx, ωy, ωz]    (角速度, rad/s)
  - 加速度计: a(t) = [ax, ay, az]   (加速度, m/s²)

输出:
  - 姿态: q_i_w(t) = [w, x, y, z]   (单位四元数)
```

### 3.2 陀螺仪积分法（基础）

#### 姿态微分方程

四元数姿态的微分方程：

```
dq/dt = 0.5 · q ⊗ [0, ω]
```

其中 `⊗` 表示四元数乘法。

写成矩阵形式：

```
┌────────┐   ┌─────────────────────────┐ ┌────┐
│ dw/dt  │   │   0    -ωx   -ωy   -ωz  │ │  w │
│ dx/dt  │ = │  ωx     0     ωz   -ωy  │ │  x │
│ dy/dt  │   │  �y    -ωz     0     ωx  │ │  y │
│ dz/dt  │   │  ωz     ωy   -ωx     0  │ │  z │
└────────┘   └─────────────────────────┘ └────┘
```

#### 离散时间积分

**一阶欧拉法**：

```
q(t + Δt) = q(t) + 0.5 · Δt · q(t) ⊗ ω(t)
q(t + Δt) = normalize(q(t + Δt))
```

**问题**：
1. 累积误差：偏差 b_g 会持续累积
2. 数值不稳定：长时间积分后姿态发散

**误差增长分析**：

```
姿态误差: e(t) ≈ b_g · t
角度误差: Δθ(t) ≈ |b_g| · t

示例: b_g = 0.01 °/s
  1分钟后: Δθ ≈ 0.6°
  10分钟后: Δθ ≈ 6°  (不可接受！)
```

### 3.3 互补滤波器

#### 基本原理

```
    陀螺仪              加速度计
       │                   │
       │              ┌────┴────┐
       │              │ 高通滤波 │ (去除直流)
       │              └────┬────┘
       │                   │
    ┌──┴───┐          ┌───┴────┐
    │ 低通滤波 │       │ 姿态估计│
    └──┬───┘          └───┬────┘
       │                   │
       └───────┬───────────┘
               ↓
              融合
               ↓
          q_estimated
```

#### 一阶互补滤波

```
q_estimated = α · q_gyro + (1 - α) · q_accel
```

**截止频率**：

```
α = τ / (τ + Δt)
```

其中 `τ` 是时间常数。

**频率响应分析**：

```
|H(ω)| 陀螺仪通道:  α / √(1 + (ωτ)²)     (低通)
|H(ω)| 加速度计通道: (1-α) · ωτ / √(1 + (ωτ)²)  (高通)
```

截止频率 `ωc = 1/τ` 处：
- 陀螺仪贡献：-3dB
- 加速度计贡献：-3dB

#### 二阶互补滤波

更好的瞬态响应：

```
H(s) = (τ₂s + 1) / (τ₁s + 1)
```

### 3.4 VQF 算法深度解析

#### 算法结构

```
                        VQF 姿态估计器
                           ↓
    ┌──────────────────────┴──────────────────────┐
    │                                             │
  高频通路                                     低频通路
  (陀螺仪积分)                                 (加速度校正)
    │                                             │
    ↓                                             ↓
  快速响应     ┌──────────────┐                漂移修正
    │          │  增益调度    │                    │
    │          │  Gain        │                    ↓
    │          │  Scheduling  │            ┌──────────────┐
    │          └──────────────┘            │   PI 控制器  │
    │                                     └──────────────┘
    └──────────────────────┬──────────────────────┘
                           ↓
                       四元数融合
                           ↓
                      q_i_w 输出
```

#### 误差计算

**重力方向误差**：

```
r_estimated = q ⊗ [0, 0, 1] ⊗ q*    (估计的重力方向)
r_measured = a / |a|                 (测量的重力方向)

error = r_measured × r_estimated     (叉积得到误差向量)
```

**物理意义**：error 的方向是使估计重力转向测量重力的旋转轴，大小是正弦值。

#### PI 控制器设计

```
// 比例项
P = Kp · error

// 积分项
I = I + Ki · error · dt

// 偏差修正
bias_correction = P + I

// 角速度校正
ω_corrected = ω_measured - bias_correction
```

**参数调整指南**：

| 参数 | 作用 | 增大 | 减小 |
|------|------|------|------|
| `Kp` | 响应速度 | 更快但震荡 | 更慢但平滑 |
| `Ki` | 稳态误差 | 消除更快 | 可能残留偏差 |

**稳定性分析**：

特征方程：
```
s² + Kp · s + Ki = 0
```

稳定性条件：
```
Kp > 0, Ki > 0
```

阻尼比：
```
ζ = Kp / (2·√Ki)
```

- `ζ > 1`：过阻尼（慢但无超调）
- `ζ = 1`：临界阻尼
- `ζ < 1`：欠阻尼（快但有超调）

推荐：`ζ ≈ 0.7`（轻微欠阻尼）

#### 增益调度（Gain Scheduling）

**问题**：静止和运动时对加速度计的信任度不同。

**解决方案**：根据运动强度调整增益。

```
// 检测运动强度
a_norm = |a| - 9.81
motion_intensity = |a_norm|

// 自适应 Kp
Kp_adaptive = Kp_base / (1 + γ · motion_intensity)

// 自可信度
confidence_accel = 1 / (1 + γ · motion_intensity)
```

**效果**：
- 静止：Kp 高，快速校正水平
- 运动：Kp 低，相信陀螺仪

### 3.5 卡尔曼滤波器（AKF）

#### 状态空间模型

**状态向量**：

```
X = [q_w, q_x, q_y, q_z, b_x, b_y, b_z]ᵀ
```

**过程模型**：

```
X(k|k-1) = Φ · X(k-1|k-1)
```

状态转移矩阵：

```
Φ = ┌─────────────────────────────────┐
    │  I₄    0.5·Δt·I₄   -0.5·Δt·I₄ │  (姿态部分)
    │  0        I₄          0       │  (偏差部分)
    └─────────────────────────────────┘
```

**测量模型**：

```
Z = H · X + V
```

测量矩阵（从加速度计得到重力方向）：

```
Z = [a_x, a_y, a_z]ᵀ / |a|
H = [重力提取矩阵, 0, 0]
```

#### 卡尔曼滤波步骤

**预测**：

```
X̂(k|k-1) = Φ · X̂(k-1|k-1)
P(k|k-1) = Φ · P(k-1|k-1) · Φᵀ + Q
```

**更新**：

```
K = P · Hᵀ · (H · P · Hᵀ + R)⁻¹          (卡尔曼增益)
X̂(k|k) = X̂(k|k-1) + K · (Z - H · X̂(k|k-1))
P(k|k) = (I - K · H) · P(k|k-1)
```

**噪声协方差**：

过程噪声 Q：反映系统不确定性
```
Q = diag([σ_q², σ_q², σ_q², σ_q², σ_b², σ_b², σ_b²])
```

测量噪声 R：反映加速度计噪声
```
R = σ_a² · I₃
```

### 3.6 算法性能对比

| 算法 | 计算复杂度 | 精度 | 延迟 | 鲁棒性 |
|------|-----------|------|------|--------|
| **陀螺仪积分** | O(1) | ⭐ | 极低 | ⭐ |
| **互补滤波** | O(1) | ⭐⭐⭐ | 低 | ⭐⭐⭐ |
| **VQF** | O(1) | ⭐⭐⭐⭐ | 低 | ⭐⭐⭐⭐ |
| **AKF** | O(n³) | ⭐⭐⭐⭐⭐ | 中 | ⭐⭐⭐⭐⭐ |

---

## 4. 滑动窗口平滑算法

### 4.1 平滑的数学基础

#### 信号处理视角

姿态序列可看作 3D 旋转空间中的信号：

```
q(t) = q_true(t) + q_jitter(t) + q_drift(t)
```

平滑的目标：去除 `q_jitter(t)`（高频抖动），保留 `q_true(t)`（真实运动）。

**频域分析**：

```
      |Q(f)|
        │
    ────┼──────────── f
        │    ╱╲    ╱╲
        │   ╱  ╲  ╱  ╲   高频抖动
        │  ╱    ╲╱    ╲
        │ ╱            ╲
        └───────────────── 低频真实运动
         │              │
       0 Hz          10 Hz
```

#### 卷积平滑

平滑本质上是一个卷积操作：

```
q_smoothed(t) = (q * w)(t) = ∫ q(τ) · w(t-τ) dτ
```

其中 `w(t)` 是平滑核函数。

### 4.2 平滑核函数

#### 均匀核（矩形窗）

```
w(t) = 1/N,  t ∈ [-T/2, T/2]
w(t) = 0,    其他
```

**频域特性**：

```
W(ω) = sinc(ωT/2) = sin(ωT/2) / (ωT/2)
```

优点：计算简单
缺点：旁瓣大（-13dB），可能产生吉布斯现象

#### 高斯核

```
w(t) = (1/√(2π)σ) · exp(-t² / (2σ²))
```

**频域特性**：

```
W(ω) = exp(-σ²ω² / 2)
```

优点：频域和时域都是高斯型，无负值
缺点：无限支持（实际需截断）

**3σ 规则**：截断到 ±3σ 保留 99.7% 能量

#### 汉宁窗（Hanning）

```
w(t) = 0.5 · (1 + cos(2πt/T)),  t ∈ [-T/2, T/2]
```

**频域特性**：

```
W(ω) = 0.5 · S(ω) + 0.25 · [S(ω-2π/T) + S(ω+2π/T)]
```

其中 `S(ω) = sinc(ωT/2)`

优点：旁瓣低（-31dB），主瓣稍宽
缺点：稍微降低频谱分辨率

#### 窗函数对比

```
时域:
矩形窗:  ┌────────┐
汉宁窗:  ╱──────╲
高斯窗:  ────●───

频域 (dB):
矩形窗:  ┐  ┌─┐ ┌─┐ ┌─  (旁瓣高)
汉宁窗:  ┐╱╲╱╲╱╲╱╲╱  (旁瓣低)
高斯窗:  ╱╲        ╱╲  (无负瓣)
        0Hz
```

### 4.3 四元数平滑

#### 问题：直接平均的问题

```
q_avg = (q0 + q1 + ... + qN) / N
```

问题：
1. 结果不是单位四元数
2. 不在四元数流形上
3. 双覆盖问题（q 和 -q 表示相同旋转）

#### 正确方法：流形上的平均

**李代数方法**：

1. 转换到李代数（切空间）：
```
θ = 2 · arccos(|q·q_ref|)
n = (q - q·q_ref·q_ref) / |q - q·q_ref·q_ref|
ξ = θ · n  (轴角表示)
```

2. 在切空间平均：
```
ξ_avg = (ξ₀ + ξ₁ + ... + ξN) / N
```

3. 转回四元数：
```
q_avg = exp(ξ_avg / 2)
```

#### SLERP 加权平均

```
q_weighted = slerp(q0, q1, w1)  // 先融合 q0, q1
q_weighted = slerp(q_weighted, q2, w2)  // 融入 q2
...
```

权重归一化：`w₀ + w₁ + ... + wN = 1`

### 4.4 滑动窗口实现

#### 对称窗口

```
窗口中心: t
窗口范围: [t - T/2, t + T/2]
姿态使用: {q(t-T/2), ..., q(t), ..., q(t+T/2)}
```

**时序图**：

```
t=0    t=1    t=2    t=3    t=4    t=5    t=6    t=7
│     │     │     │     │     │     │     │
q₀    q₁    q₂    q₃    q₄    q₅    q₆    q₇
│     │     │     │     │     │     │     │
└─────┴─────┴─────┴─────┴─────┴─────┴─────┘
      └───────── 滑动窗口 (size=4) ────┘

对 t=3 时刻:
  使用 [q₁, q₂, q₃, q₄] 进行加权平滑
  输出平滑后的 q₃'
```

#### 因果窗口（在线处理）

```
窗口中心: t
窗口范围: [t - T, t]
姿态使用: {q(t-T), ..., q(t)}
```

**延迟**：T/2

#### 非因果窗口（离线处理，FlowState 采用）

```
窗口中心: t
窗口范围: [t, t + T]
姿态使用: {q(t), ..., q(t+T)}
```

**优势**：无延迟，需要未来帧

### 4.5 窗口大小选择

#### 频域分析

窗口大小 N 对应截止频率：

```
fc ≈ fs / (2N)
```

其中 fs 是采样频率。

**示例**：fs = 30fps

| N | fc (Hz) | 平滑度 | 延迟 |
|---|---------|--------|------|
| 5 | 3 Hz | 轻微 | 0.16s |
| 10 | 1.5 Hz | 适中 | 0.33s |
| 20 | 0.75 Hz | 较强 | 0.67s |
| 30 | 0.5 Hz | 很强 | 1.00s |

#### 3dB 带宽

高斯窗的 3dB 带宽：

```
f_3dB = √(ln 2) / (2πσ) ≈ 0.187 / σ
```

### 4.6 防抖模式数学建模

#### Free Footage

```
q_smoothed = Σ w_i · q_raw(i)
```

保留所有频率成分，只衰减高频。

#### Absolute Horizon

目标：Roll = 0, Pitch = 0

```
// 提取欧拉角
[yaw, pitch, roll] = q_to_euler(q_raw)

// 强制约束
q_target = euler_to_quat([yaw, 0, 0])

// 插值
q_smoothed = slerp(q_raw, q_target, α)
```

**地平线误差定义**：

```
error_horizon = |roll| + |pitch| / 2
```

#### Virtual Gimbal

角度限制：

```
roll_clamped = clamp(roll, -roll_max, roll_max)
pitch_clamped = clamp(pitch, -pitch_max, pitch_max)
yaw_free = yaw  // 不限制
```

**等效约束矩阵**：

```
C = diag([1, 1/(1+exp(-k(roll-roll_max))), ...])
```

### 4.7 自适应平滑

#### 运动检测

```
motion_score = |dq/dt| + |d²q/dt²|
```

#### 自适应窗口大小

```
N_adaptive = N_base · (1 + k · motion_score)
```

#### 自适应核形状

```
if motion_score > threshold:
    σ = σ_fast   // 窄核，快速响应
else:
    σ = σ_slow  // 宽核，强平滑
```

---

## 5. 滚动快门校正原理

### 5.1 滚动快门的几何模型

#### CMOS 传感器曝光过程

```
时间
  │
  │  t₀      t₁      t₂      t₃      t₄
  │   │       │       │       │       │
  │   ↓       ↓       ↓       ↓       ↓
  │  ┌───┐   ┌───┐   ┌───┐   ┌───┐   ┌───┐
  │  │行0│   │行1│   │行2│   │行3│   │行4│  ← 逐行曝光
  │  └───┘   └───┘   └───┘   └───┘   └───┘
  │
  └──────────────────────────────────→ 行号
      0       1       2       3       4
```

**关键参数**：
- `t_row_readout`：单行读取时间
- `sweep_time`：整帧扫描时间
- `exposure_time`：单行曝光时间

#### 运动导致的几何失真

假设相机匀速旋转，角速度 ω：

```
行 i 的姿态: q_i = q₀ · Δq(ω · t_i)
```

其中 `Δq(θ)` 是小角度旋转四元数。

**失真可视化**：

```
静态场景 (无运动):
┌────────────────────┐
│ ────────────────   │  直线保持直线
│ ────────────────   │
│ ────────────────   │
└────────────────────┘

滚动快门 (向右运动):
┌────────────────────┐
│  ╲  ╲  ╲  ╲  ╲  ╲ │  直线变成斜线
│   ╲  ╲  ╲  ╲  ╲  ╲│
│    ╲  ╲  ╲  ╲  ╲  ╲
└────────────────────┘

果冻效应 (快速运动):
┌────────────────────┐
│ ╱╲╱╲╱╲╱╲╱╲╱╲╱╲  │  波浪状扭曲
│ ╲╱╲╱╲╱╲╱╲╱╲╱╲╱  │
│ ╱╲╱╲╱╲╱╲╱╲╱╲╱╲  │
└────────────────────┘
```

### 5.2 坐标映射关系

#### 问题定义

给定：
- 输出像素坐标：`(u_dst, v_dst)`
- 输出帧时间戳：`t_dst`
- 虚拟相机姿态：`q_vcam(t_dst)`

求：
- 输入像素坐标：`(u_src, v_src)`

#### 坐标变换链

```
(u_dst, v_dst)
    ↓
[1] 像素 → 归一化坐标
    ↓
[2] 归一化 → 3D 单位向量 (虚拟相机)
    ↓
[3] 虚拟相机 → 世界坐标
    ↓
[4] 世界 → 实际相机 (考虑滚动快门)
    ↓
[5] 实际相机 → 像素坐标
    ↓
(u_src, v_src)
```

#### 逆映射的困难

步骤 [4] 需要知道输入像素对应的时间：

```
t_src = t_dst + Δt(v_src)  ← 依赖于 v_src！
```

这是一个隐式方程，需要迭代求解。

### 5.3 迭代校正算法

#### 算法推导

**扫描因子定义**：

```
factor = v / height ∈ [0, 1]
```

**时间映射**：

```
t_row = t_first_row + factor · sweep_time
```

**姿态插值**：

```
q_row = lerp(q[t₀], q[t₀ + sweep_time], factor)
```

或高阶插值（三次样条）：
```
q_row = spline(q[t₀], q[t₁], q[t₂], q[t₃], factor)
```

#### 迭代过程

```
初始化: pos = (u_dst, v_dst)

for iter = 1 to max_iter:
    # 1. 计算扫描因子
    factor = pos.y / image_height

    # 2. 获取该行姿态
    t_row = t_first_row + factor * sweep_time
    q_row = get_pose(t_row)

    # 3. 坐标变换
    pos_3d = unproject(pos, q_vcam)         # 虚拟相机
    pos_3d_world = q_vcam * pos_3d         # 转世界
    pos_3d_cam = q_row * pos_3d_world      # 转实际相机
    pos_new = project(pos_3d_cam)          # 投影

    # 4. 收敛判断
    delta = |pos_new - pos|
    if delta < threshold:
        break

    pos = pos_new

return pos
```

#### 收敛性分析

**一阶泰勒展开**：

```
pos_{k+1} = pos_k + J · Δpos
```

其中 `J` 是雅可比矩阵。

**收敛条件**：

```
|J| < 1
```

物理意义：姿态变化足够小。

**典型收敛次数**：

| 场景 | 运动强度 | 平均迭代 | max_iter=5 足够? |
|------|---------|---------|-----------------|
| 静止 | 无 | 1-2 | 是 |
| 步行 | 低 | 2-3 | 是 |
| 跑步 | 中 | 3-4 | 是 |
| 车载 | 高 | 3-5 | 大部分是 |
| 极限运动 | 很高 | 4-6 | 可能不够 |

#### 加速技巧

**预计算姿态序列**：

```
for i = 0 to grid_size:
    t = t_first_row + i * (sweep_time / grid_size)
    quat_buffer[i] = get_pose(t)
```

迭代中使用：
```
idx = clamp(int(factor * grid_size), 0, grid_size)
q_row = quat_buffer[idx]
```

**网格大小选择**：

| grid_size | 精度 | 内存 | 推荐 |
|-----------|------|------|------|
| 20 | 低 | 小 | 快速预览 |
| 50 | 中 | 中 | **默认** |
| 100 | 高 | 大 | 高质量 |
| 200 | 很高 | 大 | 不推荐 |

### 5.4 Sweep Time 校准

#### 为什么需要校准？

标称值可能不准确：
- 温度影响
- 负载变化
- 制造公差

#### 校准方法

**方法 1：标定图案**

拍摄已知图案（如棋盘格）：

```
实际直线:  ─────────────
拍摄结果:  ~~~~~~~~~~~~  (波浪状)

测量: wavelength, amplitude

计算: sweep_time = f(wavelength, amplitude, fps)
```

**方法 2：运动测试**

已知旋转速度 ω：

```
拍摄旋转平台，测量失真角度 Δθ

sweep_time = Δθ / ω
```

**方法 3：自适应搜索**

```
for sweep_time in range(10ms, 30ms, 0.5ms):
    apply_drs(sweep_time)
    score = evaluate_quality()

选择 score 最大的 sweep_time
```

#### 裁剪比例调整

```
sweep_time_adjusted = sweep_time_original × (crop_height / sensor_height)
```

示例：
```
原始: 3000×3000, sweep_time = 23ms
裁剪: 1500×1500 (中心)
调整后: 23ms × 1500/3000 = 11.5ms
```

### 5.5 误差分析

#### 残差来源

1. **时间量化误差**：
```
Δt_quantization = sweep_time / grid_size
```

2. **插值误差**：
```
Δq_interp ≈ O(Δt²)
```

3. **收敛阈值**：
```
Δpos_convergence = threshold (通常 4 像素)
```

#### 总误差估计

```
ε_total = ε_time + ε_interp + ε_conv
        ≈ 0.1 + 0.05 + 0.5 像素
        ≈ 0.65 像素
```

---

## 6. FlowMap 生成与投影变换

### 6.1 FlowMap 的数学定义

#### 映射函数

FlowMap 定义了一个 2D 到 2D 的映射：

```
F: Ω_dst → Ω_src
   (u, v) ↦ (x(u,v), y(u,v))
```

其中：
- `Ω_dst`：输出图像定义域
- `Ω_src`：输入图像定义域

#### 向量表示

```
FlowMap[u, v] = [dx[u,v], dy[u,v]]
```

- `dx[u,v]`：x 方向位移
- `dy[u,v]`：y 方向位移

或者使用绝对坐标：
```
FlowMap[u, v] = [src_x[u,v], src_y[u,v]]
```

### 6.2 坐标系统

#### 像素坐标

```
(0, 0) ────────► (w, 0)
  │               │
  │               │
  │               │
  ▼               ▼
(0, h) ────────► (w, h)
```

#### 归一化坐标

```
[0, 0] ────────► [1, 0]
   │               │
   │               │
   │               │
   ▼               ▼
[0, 1] ────────► [1, 1]
```

转换：
```
u_norm = u / width
v_norm = v / height
```

### 6.3 投影模型详解

#### 透视投影（Pinhole）

**前向投影**（3D → 2D）：

```
u = f · X / Z + cx
v = f · Y / Z + cy
```

矩阵形式：
```
┌     ┐   ┌                 ┐ ┌     ┐
│ s·u │   │ f  0  cx  0    │ │  X  │
│ s·v │ = │ 0  f  cy  0    │ │  Y  │
│  s  │   │ 0  0   1   0    │ │  Z  │
│  1  │   │ 0  0   0   1    │ │  1  │
└     ┘   └                 ┘ └     ┘
```

**反向投影**（2D → 3D 射线）：

```
X = (u - cx) / f
Y = (v - cy) / f
Z = 1
ray = [X, Y, Z]ᵀ
```

**畸变模型**：

径向畸变：
```
r² = X² + Y²
distortion = 1 + k₁·r² + k₂·r⁴ + k₃·r⁶
```

切向畸变：
```
dx = 2·p₁·X·Y + p₂·(r² + 2·X²)
dy = p₁·(r² + 2·Y²) + 2·p₂·X·Y
```

#### 立体投影

**公式**：

```
u = 2f · X / (1 + Z) + cx
v = 2f · Y / (1 + Z) + cy
```

**性质**：

1. **共形映射**：局部保角
```
角度保持不变
```

2. **圆映射**：
```
3D 空间的圆 → 投影后仍是圆（或直线）
```

3. **最大视角**：理论上 360°（实际受镜头限制）

**逆映射**：

```
ρ = √((u-cx)² + (v-cy)²) / f
r = 2 · tan(ρ/2)

X = r · (u-cx) / ρ
Y = r · (v-cy) / ρ
Z = r · (2-ρ²) / (ρ²+4)
```

#### 等距投影

**公式**：

```
θ = atan2(Y, X)  # 方位角 [-π, π]
φ = acos(Z)      # 仰角 [0, π]

u = f · θ + cx
v = f · φ + cy
```

**性质**：

- 角度线性映射
- 适合鱼眼镜头
- 面积变形大

#### 投影对比

```
场景: 网格球面

Perspective:    ╱╲     边缘压缩
                ╱  ╲

Stereographic:  ───    均匀变形
                ╱ ╲

Equidistant:    ∩ ∩    角度保持
               ∩   ∩
```

### 6.4 低分辨率策略

#### 采样理论

**奈奎斯特定理**：

```
fs ≥ 2 · f_max
```

对 FlowMap：
```
map_frequency ≥ 2 × max_flow_gradient
```

#### 实验确定分辨率

```
测试不同分辨率:
  1920×1080 → FlowMap@960×540  (质量: 100%, 时间: 100%)
  1920×1080 → FlowMap@480×270   (质量: 98%,  时间: 25%)
  1920×1080 → FlowMap@240×135   (质量: 95%,  时间: 6%)
  1920×1080 → FlowMap@120×60    (质量: 92%,  时间: 1.5%)
```

**推荐**：`map_size = dst_size / 16`

#### 双线性插值

```
FlowMap_low(x, y) → 双线性插值 → FlowMap_high(x, y)

权重:
  w_00 = (1-dx) · (1-dy)
  w_10 = dx · (1-dy)
  w_01 = (1-dx) · dy
  w_11 = dx · dy

FlowMap_high = w_00·FlowMap_low[x,y] + w_10·FlowMap_low[x+1,y]
                + w_01·FlowMap_low[x,y+1] + w_11·FlowMap_low[x+1,y+1]
```

### 6.5 坐标变换链完整推导

#### 从输出像素到输入像素

```
# 步骤 1: 输出像素 → 归一化
(u_dst, v_dst) → (u_n, v_n)
u_n = (u_dst - cx_dst) / f_dst
v_n = (v_dst - cy_dst) / f_dst

# 步骤 2: 归一化 → 3D 单位向量 (虚拟相机)
P_vcam = unproject_stereographic(u_n, v_n)
P_vcam = [X, Y, Z]ᵀ, |P_vcam| = 1

# 步骤 3: 虚拟相机 → 世界
P_world = q_vcam * P_vcam

# 步骤 4: 世界 → 实际相机 (带滚动快门)
# 需要迭代确定 t_row 和 q_row
P_cam = q_row * P_world

# 步骤 5: 3D → 输出像素 (实际相机)
(u_src, v_src) = project_stereographic(P_cam)
u_src = f_src · X_cam / Z_cam + cx_src
v_src = f_src · Y_cam / Z_cam + cy_src
```

### 6.6 边界处理

#### 边界检测

```
def is_outside(u, v, width, height):
    return u < 0 or u >= width or v < 0 or v >= height
```

#### 填充策略

```
1. 黑色填充:
   if is_outside(u, v):
       pixel = [0, 0, 0]

2. 镜像填充:
   u_mirror = reflect(u, width)
   v_mirror = reflect(v, height)

3. 延伸填充:
   u_clamp = clamp(u, 0, width-1)
   v_clamp = clamp(v, 0, height-1)
```

---

## 7. 性能优化策略

### 7.1 并行计算

#### NEON SIMD

一次处理 4 个像素：

```
for i in range(0, rows):
    for j in range(0, cols-3, 4):  # 步长 4
        # 加载 4 个像素
        v_dst_x = [j, j+1, j+2, j+3]
        v_dst_y = [i, i,   i,   i  ]

        # 并行计算
        v_src = project_vectorized(v_dst_x, v_dst_y)

        # 存储 4 个结果
        store(map[i][j:j+4], v_src)
```

加速比：~3.5x

#### GPU 加速

**Metal Kernel**：

```metal
kernel void flowmap_kernel(
    device const float* quat_buffer [[buffer(0)]],
    device float2* map [[buffer(1)]],
    constant FlowstateParam& param [[buffer(2)]],
    uint2 gid [[thread_position_in_grid]])
{
    int i = gid.y;
    int j = gid.x;

    float2 dst_pos = float2(j, i);

    // DRS 迭代
    for (int iter = 0; iter < param.max_iter; iter++) {
        float factor = dst_pos.y * param.inv_height;
        float4 q = quat_buffer[int(factor * param.grid_size)];
        dst_pos = reproject(dst_pos, q);
    }

    map[i * param.width + j] = dst_pos;
}
```

### 7.2 内存优化

#### 缓存对齐

```
// 好的布局 (缓存友好)
struct AlignedQuaternion {
    float w, x, y, z;
} __attribute__((aligned(16)));

// 坏的布局 (缓存未命中)
struct PackedQuaternion {
    float w, x, y, z;
};
```

#### 内存池

```
class FlowMapPool:
    def __init__(self, size, n):
        self.pool = [cv::Mat(size) for _ in range(n)]
        self.available = list(range(n))

    def acquire(self):
        if self.available:
            idx = self.available.pop()
            return self.pool[idx]
        return cv::Mat(size)

    def release(self, mat):
        self.available.append(mat.index)
```

---

## 8. 参数配置指南

### 8.1 重要参数

| 参数 | 含义 | 典型值 | 影响 |
|------|------|--------|------|
| `fov` | 水平视场角 | 90-110° | 裁剪大小 |
| `winsize` | 平滑窗口 | 10-30 帧 | 防抖强度 vs 延迟 |
| `ahrs_type` | 姿态估计算法 | vqf | 精度 vs 速度 |
| `stab_type` | 防抖模式 | free-footage | 运动保留特性 |

### 8.2 场景配置

#### 运动拍摄
```bash
--stab_type=free-footage --winsize=10-15 --fov=110-120
```

#### 风景拍摄
```bash
--stab_type=absolute-horizon --winsize=25-30 --fov=90
```

---

## 9. 常见问题与解决方案

### 9.1 输出有黑边

**原因**：FOV 过大
**解决**：减小 FOV、启用动态 FOV

### 9.2 地平线倾斜

**原因**：防抖模式或 IMU 标定问题
**解决**：切换到 absolute-horizon

### 9.3 视频延迟

**原因**：平滑窗口过大
**解决**：减小 winsize

### 9.4 果冻效应

**原因**：SweepTime 错误
**解决**：校准 SweepTime

---

## 附录：快速参考

### 性能基准

| 平台 | 分辨率 | FlowMap | remap | 总计 | FPS |
|------|--------|---------|-------|------|-----|
| iPhone 13 Pro | 1080p | 1.2ms | 3ms | 4.2ms | 238 |
| iPad Pro M1 | 4K | 2.1ms | 8ms | 10.1ms | 99 |
| Desktop i7 | 1080p | 2.5ms | 5ms | 7.5ms | 133 |

---

**文档结束**

*本文档适用于团队技术分享和算法讲解，版本 3.0*
