# Insta360 防抖系统仿真程序原理详解

## 目录

1. [仿真程序概述](#1-仿真程序概述)
2. [系统架构与数据流](#2-系统架构与数据流)
3. [核心模块原理](#3-核心模块原理)
4. [动态FOV技术](#4-动态fov技术)
5. [滚动快门校正](#5-滚动快门校正)
6. [边界约束机制](#6-边界约束机制)
7. [参数调优原理](#7-参数调优原理)

---

## 1. 仿真程序概述

### 1.1 程序功能

`simulate.cpp` 是 Insta360 防抖系统的**完整仿真和测试程序**，它模拟了从原始视频到稳定视频的完整处理流程。

**核心功能**：
- 读取原始视频 + IMU 数据
- 执行姿态估计 (AHRS)
- 应用滑动窗口滤波
- 生成光流映射图
- 执行动态 FOV 调整
- 输出稳定后的视频

### 1.2 为什么需要仿真？

仿真程序是算法开发和验证的关键工具：

```
┌─────────────────────────────────────────────────┐
│              仿真的价值                           │
├─────────────────────────────────────────────────┤
│                                                 │
│  1. 快速迭代                                     │
│     ├── 修改参数立即看到效果                      │
│     ├── 无需重新编译整个系统                      │
│     └── 降低开发成本                             │
│                                                 │
│  2. 算法验证                                     │
│     ├── 验证新算法的正确性                        │
│     ├── 对比不同算法的性能                        │
│     └── 测试边界情况                             │
│                                                 │
│  3. 参数调优                                     │
│     ├── 找到最优参数组合                          │
│     ├── 理解参数间的相互影响                       │
│     └── 建立参数配置规则                          │
│                                                 │
│  4. 性能分析                                     │
│     ├── 测量各模块的计算时间                      │
│     ├── 识别性能瓶颈                             │
│     └── 验证实时性要求                           │
│                                                 │
└─────────────────────────────────────────────────┘
```

### 1.3 程序结构

文件共 1060 行，主要分为：

```
simulate.cpp
├── 头文件和配置 (1-60 行)
│   ├── 依赖库声明
│   ├── 编译选项宏定义
│   └── 全局常量配置
│
├── 工具函数 (60-300 行)
│   ├── DumpGyrodata(): 导出IMU数据
│   ├── resize(): 图像缩放
│   ├── ResizeMap(): 映射图缩放
│   ├── drawQuaternion(): 可视化姿态
│   └── RotateImage(): 图像旋转
│
├── 辅助函数 (300-440 行)
│   ├── getFootageType(): 解析防抖模式
│   ├── getAhrsType(): 解析AHRS算法
│   ├── getProjectType(): 解析投影类型
│   ├── checkHorizonDewarpShift(): 边界检查
│   └── updateProjectParameters(): 更新投影参数
│
└── 主函数 (440-1060 行)
    ├── 参数解析和配置
    ├── 元数据加载
    ├── IMU数据处理
    ├── 姿态滤波器初始化
    ├── FlowState系统初始化
    └── 逐帧处理循环
```

---

## 2. 系统架构与数据流

### 2.1 完整数据流

```
┌──────────────────────────────────────────────────────────────────────┐
│                        完整防抖处理流程                                 │
└──────────────────────────────────────────────────────────────────────┘

输入文件:
┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ video.mp4    │  │ gyro.std.txt │  │ exposure.txt │  │ metadata.json│
│ (原始视频)    │  │ (IMU数据)    │  │ (曝光时间)    │  │ (相机参数)    │
└──────┬───────┘  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘
       │                 │                 │                 │
       └─────────────────┴─────────────────┴─────────────────┘
                              │
                              ▼
                    ┌──────────────────┐
                    │  数据预处理       │
                    ├──────────────────┤
                    │ • 时间戳对齐     │
                    │ • 延迟补偿       │
                    │ • 坐标系转换     │
                    └────────┬─────────┘
                             │
                ┌────────────┴────────────┐
                │                         │
                ▼                         ▼
    ┌──────────────────┐      ┌──────────────────┐
    │  IMU 处理模块     │      │  视频帧读取      │
    ├──────────────────┤      ├──────────────────┤
    │ • 姿态估计        │      │ • 逐帧读取       │
    │ • 陀螺仪积分      │      │ • ROI裁剪       │
    │ • 传感器融合      │      │ • 格式转换       │
    └────────┬─────────┘      └────────┬─────────┘
             │                          │
             │          姿态四元数        │
             │                          │
             ▼                          │
    ┌──────────────────┐               │
    │  姿态滤波模块     │               │
    ├──────────────────┤               │
    │ • 滑动窗口平滑    │               │
    │ • 边界约束        │               │
    │ • 动态FOV计算     │               │
    └────────┬─────────┘               │
             │                          │
             │    平滑后姿态 + FOV       │
             │                          │
             └────────────┬─────────────┘
                          │
                          ▼
              ┌──────────────────────┐
              │  FlowState 模块       │
              ├──────────────────────┤
              │ • 光流计算            │
              │ • 映射图生成          │
              │ • 滚动快门校正        │
              └──────────┬───────────┘
                         │
                         │    光流映射图
                         │
                         ▼
              ┌──────────────────────┐
              │  图像重映射           │
              ├──────────────────────┤
              │ • cv::remap()         │
              │ • 双线性插值          │
              │ • 边界填充            │
              └──────────┬───────────┘
                         │
                         ▼
              ┌──────────────────────┐
              │  输出稳定视频         │
              └──────────────────────┘
```

### 2.2 模块间通信

```
┌─────────────────────────────────────────────────────────────┐
│                    模块通信机制                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   IMU处理器  ←──────→  姿态滤波器  ←──────→  FlowState系统 │
│       │                         │                  │        │
│       │ GyroData                │ Quaterniond      │        │
│       │ Quaterniond             │ Fov              │        │
│       │                         │                  │        │
│       ▼                         ▼                  ▼        │
│  [时间戳查询]              [边界计算]          [映射生成]   │
│  getSmoothedQuaternion()   calcBoxBoundry()   getFlowMap() │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 3. 核心模块原理

### 3.1 IMU 处理管理器 (ImuProcessManager)

#### 职责

IMU 处理管理器是整个系统的**基础数据源**，负责：

1. **姿态估计**：从原始 IMU 数据计算相机姿态
2. **时间插值**：在任意时间点查询姿态
3. **平滑输出**：提供滤波后的姿态

#### 姿态估计流程

```
原始 IMU 数据:
┌───┬───┬───┬───┬───┬───┬───┬───┬───┬───┐
│ t₁│ t₂│ t₃│ t₄│ t₅│ t₆│ t₇│ t₈│ t₉│t₁₀│ ...
└───┴───┴───┴───┴───┴───┴───┴───┴───┴───┘
    │                           │
    └───────────────────────────┘
           预处理和滤波
                 │
                 ▼
    ┌───┬───┬───┬───┬───┬───┬───┬───┬───┬───┐
    │ q₁│ q₂│ q₃│ q₄│ q₅│ q₆│ q₇│ q₈│ q₉│q₁₀│ ...
    └───┴───┴───┴───┴───┴───┴───┴───┴───┴───┘
    姿态四元数序列 (世界→相机旋转)

查询任意时间 t 的姿态:
    t = 7.3 秒

    1. 找到相邻时刻: t₇ = 7.0, t₈ = 7.5
    2. 四元数插值: SLERP(q₇, q₈, (t-t₇)/(t₈-t₇))
    3. 返回: q(t)
```

#### 时间戳对齐

**问题**：视频帧和 IMU 数据不同步

```
IMU 数据:    ●   ●   ●   ●   ●   ●   (200 Hz)
视频帧:      ├───────┼───────┼─────  (30 fps)

每一帧曝光时刻:
         t_frame = t_read - exposure/2 + delay

延迟组成:
┌──────────────────────────────────────────────────┐
│ delay = sensor_delay + algorithm_delay + ...     │
│                                                  │
│  sensor_delay: 传感器读出延迟 (~1ms)              │
│  algorithm_delay: 滤波算法延迟                    │
│    - VQF: ~1.0ms                                 │
│    - AKF: ~1.2ms                                 │
│  exposure/2: 曝光中心校正 (帧时间戳是曝光结束时)   │
└──────────────────────────────────────────────────┘
```

### 3.2 姿态滤波器 (SlidingWindowFilter)

#### 滑动窗口原理

```
时间轴:  t───────t───────t───────t───────t───────t
         │       │       │       │       │       │
帧索引:   0       1       2       3       4       5
                 └───────┴───────┴───────┘
                         窗口大小 = 3

当前帧: t = 3
平滑姿态 = 平均([q₁, q₂, q₃, q₄, q₅])
         └────┬──┘
         未来帧估计
```

#### 为什么需要未来帧？

**问题**：滑动窗口平滑需要对称的数据，但实时处理时未来数据未知

```
t 时刻: 只知道过去的数据
         ┌─────┐  已知
    ────┤     ├────────>
         t-3  t

解决: 预测未来姿态
    ┌─────┐  已知    ┌─────┐  预测
    ────┤     ├────────┤     ├────>
         t-3  t         t+3

预测方法:
1. 假设角速度恒定
2. 使用外推
3. 基于运动模型
```

#### 四元数平滑

**四元数不能直接平均！**

```
错误方法 ❌:
  q_avg = (q₁ + q₂ + q₃) / 3
  问题: 四元数空间是非线性的

正确方法 ✓:
  1. 选择参考四元数 q_ref (如 q₃)
  2. 计算相对旋转: Δqᵢ = qᵢ ⊗ q_ref⁻¹
  3. 转换到李代数: vᵢ = log(Δqᵢ)
  4. 向量平均: v̄ = mean(v₁, v₂, v₃)
  5. 转回四元数: q_smooth = exp(v̄) ⊗ q_ref

直观理解:
  就像求球面上点的"重心"
  需要在切空间 (李代数) 中计算
```

### 3.3 FlowState 系统

#### 光流映射生成

```
源图像坐标 ←──── 映射 ────→ 输出图像坐标

对于输出图像的每个像素 (x_out, y_out):
  1. 计算源图像坐标: (x_src, y_src) = Map(x_out, y_out)
  2. 从源图像采样: I_out(x_out, y_out) = I_src(x_src, y_src)

映射图存储:
  ┌─────────────────────────────────────────┐
  │  Map[y, x] = (x_src, y_src)             │
  │                                         │
  │  32FC2 格式: 2个通道 (x, y)             │
  │  分辨率: 40×30 (粗网格)                  │
  │                                         │
  │  使用时双线性插值到输出分辨率            │
  └─────────────────────────────────────────┘
```

#### 映射图生成流程

```
输入: 当前姿态 q(t), 投影参数

1. 生成标准网格:
   ┌───────────────────┐
   │  (0,0)  ...  (W,0)│
   │    .       .      │
   │  (0,H)  ...  (W,H)│
   └───────────────────┘

2. 应用反向投影:
   对于每个输出点:
     a. 归一化设备坐标
     b. 投影模型变换
     c. 相机姿态校正
     d. 源图像坐标

3. 存储偏移量:
   Map[x,y] = (x_src - x, y_src - y)

4. 应用到图像:
   I_out = remap(I_src, Map)
```

---

## 4. 动态FOV技术

### 4.1 什么是动态FOV？

**FOV** (Field of View) = 视场角，表示相机能看到多大范围

**动态FOV** = 根据相机运动自动调整FOV

```
静态 FOV:                    动态 FOV:
┌─────────┐                  ┌─────────┐
│ ╱╲      │                  │ ╱      │
│╱  ╲     │   固定裁剪        │╱  ╲     │   自适应裁剪
│    ╲    │                  │     ╲   │
│     ╲╱  │                  │      ╲ ╱
└─────────┘                  └─────────┘

运动时:                       运动时:
  画面边缘变黑                   自动扩大FOV
                                避免黑边
```

### 4.2 为什么需要动态FOV？

**物理原理**：

```
平滑后的姿态与真实姿态有偏差:
  q_smoothed ≠ q_raw

偏差导致的像素偏移:
  Δx = f · tan(θ_smoothed - θ_raw)
  其中 f 是焦距

问题:
  如果 |Δx| > margin (边缘余量)
  → 出现黑边

解决:
  扩大FOV → 增加 margin
  缩小FOV → 提高清晰度

权衡:
  FOV大 → 边缘少，但清晰度低
  FOV小 → 清晰度高，但可能黑边
```

### 4.3 动态FOV计算

#### FOV与运动强度的关系

```
运动强度指标:
  I(t) = |ω(t)| + α·|a(t) - g|

FOV计算:
  目标: FOV_target = FOV_base + k·I(t)

  平滑: FOV(t) = lerp(FOV(t-1), FOV_target, rate)

  约束: FOV_min ≤ FOV(t) ≤ FOV_max

参数:
  FOV_base: 基础FOV (如 80°)
  FOV_min: 最小FOV (base - range)
  FOV_max: 最大FOV (base)
  k: 增益系数
  rate: 变化率 (如 0.1)
```

#### 代码中的实现

```cpp
// 配置动态FOV范围
pose_filter->setFov(
    toRadian(source_crop_fov),      // 最大FOV (基础值)
    toRadian(source_crop_fov - dynamic_fov_range)  // 最小FOV
);

// 配置FOV变化率
double dynamic_fov_change_rate = default_dynamic_fov_rate * 30. / (fps + 1);
pose_filter->setAutoFovRate(dynamic_fov_change_rate);

// 配置FOV变化的权重
pose_filter->setLastBuffWeight(dynamic_fov_change_weight);

// 每帧获取当前FOV
double dynamic_fov;
ret = pose_filter->getDynamicFov(timestamp_sec, dynamic_fov);

// 更新投影参数
updateProjectParameters(system, distance, toDegree(dynamic_fov), ...);
```

### 4.4 不同场景的FOV策略

```
场景分类:

1. 静止 (Still)
   特征: |ω| < threshold
   策略: FOV = FOV_min (最高清晰度)

2. 平滑运动 (Smooth Motion)
   特征: 中等 |ω|, 低 |a-g|
   策略: FOV = FOV_base - 0.5·range

3. 快速运动 (Fast Motion)
   特征: 高 |ω| 或 高 |a-g|
   策略: FOV = FOV_base

4. 剧烈抖动 (Violent Shake)
   特征: |ω| > limit
   策略: FOV = FOV_max (最大容忍度)
```

---

## 5. 滚动快门校正

### 5.1 什么是滚动快门？

**全局快门 vs 滚动快门**：

```
全局快门 (Global Shutter):
  ┌───────────────────────┐
  │ 所有行同时曝光         │
  │ ────────────────────  │ ← 曝光时刻
  └───────────────────────┘

滚动快门 (Rolling Shutter):
  ┌───────────────────────┐
  │ 行1 曝光  │            │
  │   ↓                  │
  │ 行2 曝光      │        │ ← 逐行扫描
  │     ↓               │
  │ 行3 曝光         │    │
  └───────────────────────┘

常见设备: 手机、大部分消费级相机
问题: 不同行对应不同时刻的姿态
```

### 5.2 滚动快门的影响

```
无滚动快门:               有滚动快门:

  ╱│╲                      ╱│╲
 ╱ │ ╲    直线             ╱ │ ╲    波形
╱  │  ╲                  ╱  │  ╲
   │                       │

原因: 每行曝光时刻不同
      → 姿态不同
      → 投影位置不同
      → 图像扭曲
```

### 5.3 滚动快门校正方法

#### 逐行姿态计算

```
曝光时间: T_exposure
扫描时间: T_sweep = T_exposure

对于第 i 行:
  t_i = t_center + (i - H/2) / H · T_sweep

  其中:
    t_center: 帧中心时刻
    H: 总行数
    i: 行索引

姿态查询:
  q_i = getSmoothedQuaternion(t_i)

应用校正:
  对于第 i 行的每个像素:
    使用 q_i 计算映射
    而不是使用 q_center
```

#### 迭代精化

```
问题: 一次校正不够精确

解决: 多次迭代

  for iter = 1 to N:
    1. 计算当前映射图
    2. 应用映射
    3. 计算残差
    4. 更新姿态估计
    5. 重复

代码中:
  system->setDeRollingShutterIter(drs_iter_num);

  通常: N = 5 次
```

### 5.4 扫描模式

```
垂直扫描 (Vertical Sweep):
  ┌───────────────────────┐
  │ ↓ ↓ ↓ ↓ ↓ ↓ ↓ ↓ ↓ ↓  │
  │ ↓ ↓ ↓ ↓ ↓ ↓ ↓ ↓ ↓ ↓  │
  │ ↓ ↓ ↓ ↓ ↓ ↓ ↓ ↓ ↓ ↓  │
  └───────────────────────┘
  从上到下

水平扫描 (Horizontal Sweep):
  ┌───────────────────────┐
  │ → → → → → → → → → →  │
  │ → → → → → → → → → →  │
  │ → → → → → → → → → →  │
  └───────────────────────┘
  从左到右

Insta360 主要使用: 垂直扫描
```

---

## 6. 边界约束机制

### 6.1 为什么需要边界约束？

**问题**：过度平滑导致画面与实际不同步

```
实际相机指向:    →→→→→→→
渲染画面指向:    →→→→→→→  (滞后)

结果:
  ┌────────────┐
  │            │  ← 黑边
  │  ╱╲        │
  │ ╱  ╲       │
  │     ╲      │
  │      ╲╱   │  ← 黑边
  └────────────┘
```

### 6.2 约束类型

#### 角度约束

```
原则: 平滑姿态与原始姿态的夹角不超过阈值

|q_smoothed - q_raw| ≤ θ_max

角度阈值配置:
  • 默认: 10°
  • 4:3 比例: 7.5° (横向空间大)
  • 16:9 比例: 10°
  • 地平线锁定: 22.5°

计算:
  θ = 2 · arccos(|q_smoothed · q_raw|)
```

#### 横滚约束

```
横滚角 (Roll) = 绕光轴旋转

特殊处理原因:
  • 人眼对横滚敏感 (地平线倾斜)
  • 需要更严格的约束

不同模式:
  • Free Footage: Roll < 20°
  • Full Directional: Roll < 5°
  • Still: Roll < 2°
  • Horizon Lock: Roll < 1°
```

#### 边界框约束

```
约束可用像素范围:

  原始图像:                边界框:
  ┌──────────────┐        ┌──────┐
  │              │        │  ╱╲  │
  │    ╱╲        │        │ ╱  ╲ │
  │   ╱  ╲       │   →    │     ╲│
  │       ╲      │        │      ╲╱
  │        ╲╱    │        └──────┘
  └──────────────┘

约束:
  • 窗口中心不能超出边界框
  • 边界框大小基于 FOV 和最大旋转角计算

计算:
  box_size = crop_size - margin
  margin = f · tan(θ_max) + drs_margin
```

### 6.3 边界检查

```
逐帧检查:

  1. 计算整个曝光窗口的姿态变化
     Δq = q(t_start) ⊗ q(t_end)⁻¹

  2. 检查边界框
     for each line in sweep:
       q_line = q(t_line)
       if line_out_of_boundary(q_line):
         return OUT_OF_BOUNDARY

  3. 如果超出边界:
     • 降低平滑强度
     • 缩小窗口大小
     • 或使用姿态本身 (无平滑)
```

---

## 7. 参数调优原理

### 7.1 关键参数及其影响

```
┌──────────────────────────────────────────────────────────┐
│                    参数调优矩阵                            │
├────────────┬─────────────────────────────────────────────┤
│ 参数       │ 影响和调优策略                                │
├────────────┼─────────────────────────────────────────────┤
│ fov        │ 基础视场角                                    │
│            │ • 小→ 清晰度高，易出黑边                      │
│            │ • 大→ 黑边少，清晰度低                        │
│            │ 推荐: 根据输出比例选择                         │
│            │   - 16:9 → 90-100°                          │
│            │   - 4:3 → 80-90°                            │
├────────────┼─────────────────────────────────────────────┤
│ winsize    │ 滑动窗口大小 (帧数)                           │
│            │ • 小→ 响应快，平滑弱                          │
│            │ • 大→ 平滑强，延迟高                          │
│            │ 推荐:                                        │
│            │   - 静止/慢速 → 15-25                        │
│            │   - 快速运动 → 5-10                          │
├────────────┼─────────────────────────────────────────────┤
│ dynamic_   │ 动态FOV范围 (度)                              │
│ fov_range  │ • 大→ 更少黑边，更频繁FOV变化                  │
│            │ • 小→ 稳定FOV，可能黑边                       │
│            │ 推荐: 3-5°                                  │
├────────────┼─────────────────────────────────────────────┤
│ horizon_   │ 地平线矫正角度 (度)                            │
│ angle      │ • 小→ 更稳定，横滚校正弱                      │
│            │ • 大→ 更强横滚校正，可能不自然                 │
│            │ 推荐:                                        │
│            │   - 手持平移 → 20-30°                        │
│            │   - 第一人称 → 40-50°                        │
├────────────┼─────────────────────────────────────────────┤
│ max_roll_  │ 最大横滚角 (度)                               │
│ angle      │ • 限制横滚方向旋转角度                         │
│            │ 推荐: 5-15°                                 │
├────────────┼─────────────────────────────────────────────┤
│ distance   │ 投影距离 (立体投影)                            │
│            │ • 0 → 针孔投影                                │
│            │ • >0 → 立体投影                               │
│            │ 推荐:                                        │
│            │   - 针孔 → 0.0                               │
│            │   - 立体 → 0.5-1.0                           │
├────────────┼─────────────────────────────────────────────┤
│ stab_lvl   │ 防抖等级 (0-3)                                │
│            │ • 0 → 低防抖 (动态FOV响应快)                  │
│            │ • 1 → 标准防抖                                │
│            │ • 2 → 高防抖                                  │
│            │ • 3 → 地平线锁定                              │
├────────────┼─────────────────────────────────────────────┤
│ drs_iter   │ 滚动快门迭代次数                               │
│            │ • 多→ 更精确，计算量大                         │
│            │ • 少→ 快速，可能有残差                         │
│            │ 推荐: 3-5                                   │
└────────────┴─────────────────────────────────────────────┘
```

### 7.2 不同场景的推荐配置

```
场景1: 三脚架拍摄
  目标: 最大稳定性
  配置:
    - stab_type: still
    - winsize: 20
    - horizon_angle: 0
    - max_roll_angle: 5
    - fov: 基础值

场景2: 行走拍摄
  目标: 平滑 + 自然
  配置:
    - stab_type: z-directional
    - winsize: 10-15
    - horizon_angle: 15-20
    - max_roll_angle: 10
    - dynamic_fov_range: 3-5

场景3: 跑步/运动
  目标: 稳定性优先
  配置:
    - stab_type: full-directional
    - winsize: 5-8
    - horizon_angle: 30-40
    - max_roll_angle: 15
    - dynamic_fov_range: 5

场景4: 第一人称
  目标: 保留运动感
  配置:
    - stab_type: free-footage
    - winsize: 3-5
    - horizon_angle: 45
    - max_roll_angle: 20
    - dynamic_fov_range: 5-7
```

### 7.3 FPS相关调整

```
FPS 对参数的影响:

  延迟补偿:
    delay ∝ 1/fps
    高FPS → 需要更小的延迟

  窗口大小:
    winsize (帧) ∝ fps
    保持时间窗口恒定:
      winsize_60fps = 20
      winsize_30fps = 10
      winsize_120fps = 40

  动态FOV变化率:
    rate ∝ 1/fps
    保持每度变化时间恒定:
      rate_60fps = 0.1
      rate_30fps = 0.2
      rate_120fps = 0.05

代码实现:
  double fps_factor = 60.0 / (fps + 1);
  winsize_adj = base_winsize * fps_factor;
  rate_adj = base_rate * fps_factor;
```

---

## 8. 调试与可视化

### 8.1 姿态可视化

```
drawQuaternion() 函数:

在图像角点绘制坐标系:

  ┌────────────────┐
  │  ↑ Y (绿色)    │
  │  │             │
  │  └──→ X (红色) │
  │   ↖ Z (蓝色)   │
  └────────────────┘

  原始姿态 (左上)        平滑姿态 (右下)

  指向相机坐标系        指向虚拟相机坐标系
  的世界坐标轴          的世界坐标轴

用途:
  • 可视化相机运动
  • 对比原始vs平滑
  • 验证姿态计算
```

### 8.2 边界框可视化

```
在源图像上绘制约束边界框:

  ┌──────────────────┐
  │  ┌────────────┐  │
  │  │ 红色边界框  │  │
  │  │            │  │
  │  │    ╱╲      │  │
  │  │   ╱  ╲     │  │
  │  │       ╲    │  │
  │  │        ╲╱  │  │
  │  └────────────┘  │
  └──────────────────┘

用途:
  • 验证边界计算
  • 检查是否超出约束
  • 调试边界参数
```

### 8.3 信息显示

```
在输出图像上显示参数:

  sweep: 25.5ms;           扫描时间
  delay: -50.0ms;          延迟补偿
  winsize: 20;             窗口大小
  fov: 85.3°               当前FOV

用途:
  • 实时监控参数
  • 调试时间同步
  • 验证动态FOV
```

---

## 9. 总结

### 9.1 系统设计的核心思想

```
1. 传感器融合
   IMU提供粗略姿态 → 视觉提供精细校正
   快速响应 + 长期稳定

2. 分层处理
   预处理 → 姿态估计 → 滤波 → 渲染
   每层专注单一职责

3. 自适应调整
   动态FOV、边界约束、运动检测
   根据场景自动优化

4. 实时性优先
   滑动窗口、缓存管理、迭代控制
   保证帧率要求

5. 可配置性
   丰富的参数接口
   适应不同场景需求
```

### 9.2 关键技术点

| 技术 | 原理 | 作用 |
|------|------|------|
| **四元数平滑** | 李代数平均 | 避免万向锁，平滑插值 |
| **滑动窗口** | 时间局部平均 | 消除高频抖动 |
| **动态FOV** | 自适应视场 | 平衡清晰度与黑边 |
| **滚动快门校正** | 逐行姿态 | 消除扫描畸变 |
| **边界约束** | 角度/像素限制 | 保证视觉合理 |
| **光流映射** | 像素重映射 | 非线性变换 |

### 9.3 可扩展方向

```
1. 深度学习融合
   • 使用网络预测动态FOV
   • 学习最优滤波参数

2. 多相机协同
   • 利用多相机约束
   • 提高边界精度

3. 场景理解
   • 识别运动模式
   • 自适应选择防抖策略

4. 实时优化
   • GPU加速光流计算
   • 并行化姿态估计
```

---

*本文档详细解释了 simulate.cpp 仿真程序的原理，涵盖系统架构、核心算法、参数调优等内容。侧重于物理原理和数学基础，避免大量代码细节。*
