# 卷帘快门校正算法深度解析

## 目录

1. [卷帘快门物理原理](#1-卷帘快门物理原理)
2. [卷帘快门畸变机制](#2-卷帘快门畸变机制)
3. [畸变数学模型](#3-畸变数学模型)
4. [校正算法原理](#4-校正算法原理)
5. [Insta360实现详解](#5-insta360实现详解)
6. [参数配置与优化](#6-参数配置与优化)
7. [高级优化技术](#7-高级优化技术)

---

## 1. 卷帘快门物理原理

### 1.1 图像传感器基础

#### 图像传感器结构

```
CMOS传感器平面布局:

┌────────────────────────────────────────────────────────────┐
│                      镜头                                   │
│                        ↓                                   │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                   颜色滤镜阵列 (CFA)                  │  │
│  │  ┌───┬───┬───┬───┬───┬───┬───┬───┐                  │  │
│  │  │ R │ G │ R │ G │ R │ G │ R │ G │  Bayer Pattern   │  │
│  │  ├───┼───┼───┼───┼───┼───┼───┼───┤                  │  │
│  │  │ G │ B │ G │ B │ G │ B │ G │ B │                  │  │
│  │  ├───┼───┼───┼───┼───┼───┼───┼───┤                  │  │
│  │  │ R │ G │ R │ G │ R │ G │ R │ G │                  │  │
│  │  └───┴───┴───┴───┴───┴───┴───┴───┘                  │  │
│  └──────────────────────────────────────────────────────┘  │
│                        ↓                                   │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                   像素单元阵列                        │  │
│  │  ┌─────┬─────┬─────┬─────┬─────┬─────┐               │  │
│  │  │ P₀₀ │ P₀₁ │ P₀₂ │ P₀₃ │ ... │ P₀ₙ │  Row 0         │  │
│  │  ├─────┼─────┼─────┼─────┼─────┼─────┤               │  │
│  │  │ P₁₀ │ P₁₁ │ P₁₂ │ P₁₃ │ ... │ P₁ₙ │  Row 1         │  │
│  │  ├─────┼─────┼─────┼─────┼─────┼─────┤               │  │
│  │  │ ... │ ... │ ... │ ... │ ... │ ... │               │  │
│  │  ├─────┼─────┼─────┼─────┼─────┼─────┤               │  │
│  │  │ Pₘ₀ │ Pₘ₁ │ Pₘ₂ │ Pₘ₃ │ ... │ Pₘₙ │  Row m         │  │
│  │  └─────┴─────┴─────┴─────┴─────┴─────┘               │  │
│  └──────────────────────────────────────────────────────┘  │
│                        ↓                                   │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                  读出电路                             │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────┘
```

#### 像素单元工作原理

每个像素单元包含：
- **光电二极管**：将光子转换为电子
- **复位晶体管**：清空电荷
- **源极跟随器**：读取信号
- **行选通**：选择读取哪一行

### 1.2 全局快门 vs 卷帘快门

#### 全局快门 (Global Shutter)

```
时间轴:

t₀      t₁      t₂      t₃      t₄
│       │       │       │       │
├───────┴───────┴───────┴───────┤  曝光阶段
        ↑
    所有像素同时开始曝光

t₄      t₅      t₆      t₇      t₈
│       │       │       │       │
├───────┴───────┴───────┴───────┤  读出阶段
        ↑
    所有像素同时读取

特点:
• 所有像素在同一时刻曝光
• 同一时刻结束曝光
• 理论上无时间偏差
• 成本高、功耗大（需要额外存储）
```

#### 卷帘快门 (Rolling Shutter)

```
时间轴:

Row 0:  ┌────┐    ┌────┐    ┌────┐
         │Exp │    │Read│    │Exp │
         └────┘    └────┘    └────┘
         t₀→t₁     t₁→t₂     t₈→t₉

Row 1:     ┌────┐    ┌────┐    ┌────┐
            │Exp │    │Read│    │Exp │
            └────┘    └────┘    └────┘
            t₁→t₂     t₂→t₃     t₉→t₁₀

Row 2:       ┌────┐    ┌────┐    ┌────┐
               │Exp │    │Read│    │Exp │
               └────┘    └────┘    └────┘
               t₂→t₃     t₃→t₄     t₁₀→t₁₁

Row n:                       ┌────┐    ┌────┐
                              │Exp │    │Read│
                              └────┘    └────┘
                              tₙ→tₙ₊₁  tₙ₊₁→tₙ₊₂

特点:
• 逐行曝光
• 逐行读出
• 每行有时间偏差 Δt = t_row_read
• 成本低、功耗小
• 大多数消费级相机使用

扫描时间:
  T_sweep = T_read_per_row × N_rows

典型值:
  • T_read_per_row ≈ 10-20 μs
  • N_rows ≈ 2000-4000
  • T_sweep ≈ 20-80 ms
```

### 1.3 卷帘快门的物理实现

#### 读出架构

```
列总线读出架构:

┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  Row 0  →─┬──[ADC]──┬──[ADC]──┬──[ADC]──┬── ... ─→ 输出     │
│           │         │         │         │                   │
│  Row 1  →─┼──[ADC]──┼──[ADC]──┼──[ADC]──┼── ... ─→ 输出     │
│           │         │         │         │                   │
│  Row 2  →─┼──[ADC]──┼──[ADC]──┼──[ADC]──┼── ... ─→ 输出     │
│           │         │         │         │                   │
│   ...    ─┼─ ... ───┼─ ... ───┼─ ... ───┼─ ... ─→ 输出     │
│           │         │         │         │                   │
│  Row N  →─┴──[ADC]──┴──[ADC]──┴──[ADC]──┴── ... ─→ 输出     │
│                                                             │
│  控制逻辑:                                                  │
│  1. 复位所有像素                                           │
│  2. 启动 Row 0 曝光                                        │
│  3. 延迟 Δt                                                │
│  4. 启动 Row 1 曝光                                        │
│  5. ...                                                   │
│  6. 同时读出已完成曝光的行                                 │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 时序控制

```
卷帘快门时序图:

           Row 0    Row 1    Row 2    Row 3
           │  │     │  │     │  │     │  │
复位:      █  │     █  │     █  │     █  │
           │  │     │  │     │  │     │  │
曝光:      ████     │ ████    │ │ ████  │ │ │ ████
           │  │     │  │     │  │     │  │
读出:      │  ████  │  │ ████│  │ │ ████│  │ │ │ ████
           │  │     │  │     │  │     │  │
           t₀ t₁    t₂ t₃    t₄ t₅    t₆ t₇  时间

Δt_exposure = t₁ - t₀  (曝光时间)
Δt_row = t₂ - t₀        (行延迟)

典型值:
  Δt_exposure = 1-33 ms (取决于帧率和光照)
  Δt_row = 10-20 μs
```

---

## 2. 卷帘快门畸变机制

### 2.1 畸变的物理成因

#### 相机运动导致的畸变

```
场景: 相机向右平移拍摄垂直线条

全局快门结果:
  │   │   │   │   │
  │   │   │   │   │
  │   │   │   │   │
  │   │   │   │   │

卷帘快门结果:
  ╲   ╲   ╲   ╲   ╲
   ╲   ╲   ╲   ╲   ╲
    ╲   ╲   ╲   ╲   ╲
     ╲   ╲   ╲   ╲   ╲

原因:
Row 0 曝光时刻: t₀  → 相机在位置 x₀
Row 1 曝光时刻: t₁  → 相机在位置 x₁
Row 2 曝光时刻: t₂  → 相机在位置 x₂
...

如果相机向右移动:
  x₀ < x₁ < x₂ < ...

每个行看到的物体位置不同
→ 图像扭曲
```

#### 旋转导致的畸变

```
场景: 相机绕Y轴旋转拍摄网格

理想图像 (无旋转):
┌──────────────────────────┐
│ ────┼────┼────┼────┼───│
│ ────┼────┼────┼────┼───│
│ ────┼────┼────┼────┼───│
│ ────┼────┼────┼────┼───│
│ ────┼────┼────┼────┼───│
└──────────────────────────┘
│  │  │  │  │  │  │  │  │
  垂直线

卷帘快门图像 (顺时针旋转):
┌──────────────────────────┐
│ ╲   ╲   ╲   ╲   ╲   ╲  │
│  ╲   ╲   ╲   ╲   ╲   ╲ │
│   ╲   ╲   ╲   ╲   ╲   ╲│
│    ╲   ╲   ╲   ╲   ╲   ╲
│     ╲   ╲   ╲   ╲   ╲  ╲
└──────────────────────────┘
 ╲  ╲  ╲  ╲  ╲  ╲  ╲  ╲
  斜线 (向右倾斜)

原因:
Row 0 曝光时: 相机角度 θ₀
Row 1 曝光时: 相机角度 θ₁ = θ₀ + ω·Δt
Row 2 曝光时: 相机角度 θ₂ = θ₁ + ω·Δt
...

不同行看到不同角度的场景
→ 垂直线变成斜线
```

### 2.2 畸变的类型

#### 平移畸变 (Translation Distortion)

```
特征:
• 图像"倾斜"或"剪切"
• 直线保持直线
• 类似于仿射变换

数学描述:
  x_distorted = x + v_x·t(y)
  y_distorted = y + v_y·t(y)

其中:
  (v_x, v_y) 是速度
  t(y) 是第y行的曝光时刻

示例: 水平平移
原始:  →→→→→→→→
       →→→→→→→→
       →→→→→→→→

畸变:  ↘↘↘↘↘↘↘
        →→→→→→→→
        ↗↗↗↗↗↗↗
```

#### 旋转畸变 (Rotation Distortion)

```
特征:
• 图像"弯曲"或"扇形化"
• 直线变成曲线
• 局部旋转效应

数学描述:
  θ_distorted = θ(y) = θ₀ + ω·t(y)

示例: 绕Y轴旋转
原始:  │  │  │  │  │
       │  │  │  │  │
       │  │  │  │  │

畸变:  ╲  ╲  ╲  ╲  ╲
        │  │  │  │  │
        ╱  ╱  ╱  ╱  ╱
```

#### 振动畸变 (Vibration Distortion)

```
特征:
• 高频波动
• 局部扭曲
• "果冻效应"(Jello Effect)

示例: 高频手抖
原始:  ──────────────
       ──────────────
       ──────────────

畸变:  ∿∿∿∿∿∿∿∿∿∿∿∿∿
       ∿∿∿∿∿∿∿∿∿∿∿∿∿
       ∿∿∿∿∿∿∿∿∿∿∿∿∿

原因:
• 相机快速抖动
• 每行看到不同位置/角度
• 人眼看起来像"果冻"晃动
```

### 2.3 畸变的影响因素

#### 帧率 (FPS)

```
低帧率 (30 fps):
  每帧: 33.3 ms
  扫描时间: ~20 ms
  占比: 60%
  → 畸变明显

高帧率 (120 fps):
  每帧: 8.3 ms
  扫描时间: ~20 ms
  占比: 240% (!)
  → 更严重的畸变

反直觉: 高帧率不一定减少卷帘快门效应!
```

#### 分辨率

```
低分辨率 (1080p):
  行数: 1080
  扫描时间: ~15 ms

高分辨率 (4K):
  行数: 2160
  扫描时间: ~30 ms

8K 分辨率:
  行数: 4320
  扫描时间: ~60 ms

结论: 分辨率越高，卷帘快门效应越明显
```

#### 曝光时间

```
短曝光 (1/120s):
  运动冻结 → 帧间运动大
  → 帧间抖动明显

长曝光 (1/30s):
  运动模糊 → 帧内运动平均
  → 帧内畸变平均

权衡:
  短曝光: 清晰但有果冻效应
  长曝光: 模糊但果冻效应减轻
```

---

## 3. 畸变数学模型

### 3.1 行时间模型

#### 时间戳定义

```
帧时间戳定义:

  t_frame (文件中记录)
    │
    ├─ 帧读出完成时刻
    │
    └─ 不等于帧中心时刻!

正确的行时间戳:

  t_center = t_frame - T_sweep/2 - T_delay
  t_row(y) = t_center + (y - H/2) / H × T_sweep

其中:
  T_sweep = 扫描时间 (所有行的曝光时间跨度)
  T_delay = 系统延迟
  H = 图像高度 (像素)
  y = 行索引 [0, H-1]

可视化:

        t_frame
           │
           │    ← 文件记录的时间戳
           │
    ┌──────┴──────┐
    │   扫描期间    │
    │ ──┬──┬──┬──┬ │
    │ 0 │ 1 │ 2 │...│  行
    │ ──┴──┴──┴──┴ │
    └──────────────┘
           │
        t_center  ← 帧中心时刻
```

#### 姿态插值

```
已知姿态序列:

  q₀      q₁      q₂      q₃      q₄
  ●       ●       ●       ●       ●
  │       │       │       │       │
  t₀     t₁      t₂      t₃      t₄

查询任意时刻 t 的姿态:

  1. 找到相邻时刻: tᵢ ≤ t ≤ tᵢ₊₁

  2. 球面线性插值 (SLERP):
     α = (t - tᵢ) / (tᵢ₊₁ - tᵢ)
     q(t) = SLERP(qᵢ, qᵢ₊₁, α)

  3. 或者使用更高级的插值:
     - 三次样条插值
     - 高斯过程插值
     - 卡尔曼滤波平滑
```

### 3.2 畸变模型

#### 像素运动方程

```
设:
  p = [x, y]ᵀ  → 像素坐标
  R(t)         → 相机旋转矩阵
  t(t)         → 相机平移向量
  λ            → 深度

世界点到图像平面的投影:

  π(p, t) = K · [R(t) | t(t)] · P_world

其中:
  K 是相机内参矩阵
  P_world 是世界坐标

卷帘快门效应:
  第 y 行看到的时刻是 t(y)

  p_distorted = π(p, t(y))

  而不是:
  p_ideal = π(p, t_center)
```

#### 旋转畸变模型

```
简化模型: 只考虑旋转，忽略平移

对于像素 p = [x, y]ᵀ:

  1. 归一化图像坐标:
     p_norm = K⁻¹ · [x, y, 1]ᵀ

  2. 计算第 y 行的旋转:
     Δθ = ω · (t(y) - t_center)
     R_row = R_center · exp(Δθ)

  3. 应用旋转:
     p_distorted = R_row · p_norm

  4. 重新投影:
     p_final = K · p_distorted

矩阵形式:
  p_distorted = K · R(y) · K⁻¹ · p
              = K · [R_center · exp(ω·Δt(y))] · K⁻¹ · p
```

#### 小角度近似

```
当 Δθ 很小时 (例如 < 5°):

  exp(Δθ) ≈ I + [Δθ]×

其中 [Δθ]× 是反对称矩阵:

  [Δθ]× = [  0     -Δθ_z   Δθ_y  ]
          [ Δθ_z    0     -Δθ_x  ]
          [-Δθ_y   Δθ_x    0     ]

一阶近似:

  p_distorted ≈ p + J · Δθ

其中 J 是雅可比矩阵:

  J = ∂p/∂θ |_{θ=0}
```

### 3.3 扫描时间计算

#### 从相机参数计算

```
已知:
  • 帧率: fps
  • 分辨率: W × H
  • 读出速度: R (像素/秒)

扫描时间:

  T_sweep = H / R

读出速度估算:

  R = f_read × N_parallel

  其中:
    f_read = 单个ADC的读出频率 (~10-50 MHz)
    N_parallel = 并行ADC数量 (2-16)

典型值:
  f_read = 40 MHz
  N_parallel = 8
  R = 320 Mpixels/s

  4K (3840×2160):
    T_sweep = 2160 / 320M ≈ 6.8 ms

  8K (7680×4320):
    T_sweep = 4320 / 320M ≈ 13.5 ms
```

#### 从曝光时间估算

```
通常:
  T_sweep ≈ k × T_exposure

其中 k ≈ 1.5-3

原因:
  • 曝光时间不能短于扫描时间
  • 扫描时间与读出架构相关

实际测量方法:
  1. 快速旋转相机 (已知角速度)
  2. 观察图像中垂直线的倾斜角度
  3. 计算: T_sweep = Δθ / ω
```

---

## 4. 校正算法原理

### 4.1 校正的基本思路

#### 问题重述

```
已知:
  • 畸变图像 I_distorted
  • 每行的曝光时间 t(y)
  • 每个时刻的相机姿态 R(t)

目标:
  • 重建无畸变图像 I_corrected

挑战:
  • 需要知道精确的行时间
  • 需要知道精确的相机姿态
  • 需要处理遮挡和孔洞
```

#### 逆向映射法

```
正向映射 (Forward Warping):

  对于源图像每个像素 p_s:
    计算目标位置 p_d = W(p_s)
    复制: I_d(p_d) = I_s(p_s)

  问题:
    • 可能出现空洞
    • 可能出现重叠
    • 需要复杂的融合

逆向映射 (Inverse Warping) ✓:

  对于目标图像每个像素 p_d:
    计算源位置 p_s = W⁻¹(p_d)
    插值: I_d(p_d) = interp(I_s, p_s)

  优势:
    • 无空洞
    • 易于并行
    • 使用标准插值 (双线性/双三次)
```

### 4.2 单步校正算法

#### 算法流程

```
输入:
  • 畸变图像 I_distorted
  • 姿态轨迹 {q₀, q₁, ..., qₙ}
  • 行时间函数 t(y)

输出:
  • 校正图像 I_corrected

算法:

  1. 预处理:
     计算中心时刻姿态:
       q_center = q(t_center)

  2. 对于目标图像每个像素 p_d = [x, y]ᵀ:

     a. 归一化:
        p_norm = K⁻¹ · [x, y, 1]ᵀ

     b. 计算第 y 行在曝光时刻的姿态:
        t_row = t_center + (y - H/2) / H × T_sweep
        q_row = interpolate(q_trajectory, t_row)

     c. 计算从行姿态到中心姿态的变换:
        Δq = q_center ⊗ q_row⁻¹

     d. 应用逆旋转:
        p_src_norm = rotate(p_norm, Δq)

     e. 重新投影到源图像:
        p_src = K · p_src_norm

     f. 双线性插值:
        I_corrected(y, x) = bilinear(I_distorted, p_src)

  3. 边界处理:
     填充黑色或镜像
```

#### 图示说明

```
源图像 (畸变):

  ╲  ╲  ╲  ╲  ╲  ╲
   ╲  ╲  ╲  ╲  ╲  ╲
    ╲  ╲  ╲  ╲  ╲  ╲

每个目标像素 p_d:

  1. 找到源图像位置 p_s

     p_s  ●────┐
          ╲    │
           ╲   │  Δq
            ╲  │
             ╲ ↓
              p_d

  2. 从源图像采样

  3. 填充到目标位置

目标图像 (校正):

  │  │  │  │  │  │
  │  │  │  │  │  │
  │  │  │  │  │  │
```

### 4.3 迭代精化算法

#### 为什么需要迭代？

```
单步校正的问题:

  假设:
    • 已知精确的相机姿态
    • 姿态与像素位置无关

  实际:
    • 姿态估计可能有误差
    • 姿态可能来自光流/特征匹配
    • 特征位置受畸变影响
    → 鸡生蛋、蛋生鸡问题

解决方案: 迭代

  迭代 0: 使用粗略姿态 (IMU)
  迭代 1: 基于校正图像重新估计姿态
  迭代 2: 使用精化姿态重新校正
  ...
  收敛: 姿态不再显著变化
```

#### 迭代算法流程

```
输入:
  • 畸变图像 I_distorted
  • 初始姿态轨迹 q^(0)(t)
  • 迭代次数 N

初始化:
  q_est = q^(0)

for iter = 1 to N:

  1. 使用当前姿态估计校正图像:
     I_temp = undistort(I_distorted, q_est)

  2. 在校正图像上追踪特征:
     features_temp = track_features(I_temp)

  3. 反向投影到原始图像:
     features_orig = project_back(features_temp, q_est)

  4. 在原始图像上精化特征位置:
     features_refined = refine_features(
       I_distorted,
       features_orig
     )

  5. 更新姿态估计:
     q_est = refine_pose(features_refined, q_est)

输出:
  I_corrected = undistort(I_distorted, q_est)
```

#### 收敛准则

```
停止条件 (满足任一):

  1. 达到最大迭代次数:
     iter ≥ N_max
     通常 N_max = 5-10

  2. 姿态变化小于阈值:
     ||q^(k+1) - q^(k)|| < ε_q
     通常 ε_q ≈ 0.1°

  3. 图像变化小于阈值:
     ||I^(k+1) - I^(k)|| < ε_I
     通常 ε_I ≈ 1-2 像素

  4. 光流残差收敛:
     residual = Σ||flow^(k+1) - flow^(k)||²
     residual < ε_r
```

### 4.4 多帧联合优化

#### 全局优化方法

```
思路: 同时优化多帧的姿态

目标函数:

  min Σᵢ Σⱼ ||π(pⱼ, qᵢ) - pⱼᵒᵇˢ||²
   q₁...qₙ   i  j

       + λ·||qᵢ - qᵢᴵᴹᵁ||²
       + μ·smoothness_regularization(q)

其中:
  • 第一项: 重投影误差
  • 第二项: IMU先验
  • 第三项: 平滑正则化

求解:
  • 雅可比矩阵构建
  • 稀疏线性求解器
  • 高斯-牛顿或LM算法
```

#### 滑动窗口优化

```
原因: 全局优化计算量太大

滑动窗口方法:

  窗口: k 帧 (例如 k=10)

  ┌─────────────────────────────────┐
  │ f₁ f₂ f₃ f₄ f₅ f₆ f₇ f₈ f₉ f₁₀ │
  │ └───────────┘                   │
  │   当前优化窗口                   │
  │      └───────────┘              │
  │         下一帧                  │
  └─────────────────────────────────┘

  1. 优化窗口内k帧的姿态
  2. 固定第1帧 (边缘化)
  3. 添加第k+1帧
  4. 重复

优势:
  • 计算量可控
  • 实时性能好
  • 局部一致性
```

---

## 5. Insta360实现详解

### 5.1 系统架构

#### 整体流程

```
Insta360 卷帘快门校正流程:

┌─────────────────────────────────────────────────────────┐
│                                                         │
│  输入:                                                  │
│  ├── 原始视频帧                                          │
│  ├── IMU数据 (陀螺仪 + 加速度计)                         │
│  ├── 曝光时间 (.exposure.txt)                            │
│  └── 相机内参                                            │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 1. 姿态估计模块                                  │   │
│  │    • VQF/AKF/Madgwick 融合算法                   │   │
│  │    • 输出: 高频姿态序列 (200-1000 Hz)            │   │
│  │    • 缓存: 滑动窗口 (几秒)                       │   │
│  └────────────────────┬────────────────────────────┘   │
│                         │                                │
│                         ▼                                │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 2. 时间戳对齐模块                                │   │
│  │    • 调整延迟: delay_time (可调)                 │   │
│  │    • 计算帧中心: t_center = t_read - T_exp/2     │   │
│  │    • 计算扫描时间: T_sweep = sweep_time         │   │
│  └────────────────────┬────────────────────────────┘   │
│                         │                                │
│                         ▼                                │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 3. 姿态滤波模块                                  │   │
│  │    • 滑动窗口平滑                                │   │
│  │    • 边界约束                                    │   │
│  │    • 输出: 平滑姿态 + 动态FOV                     │   │
│  └────────────────────┬────────────────────────────┘   │
│                         │                                │
│                         ▼                                │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 4. FlowState模块                                 │   │
│  │    • 光流计算 (IMU引导)                          │   │
│  │    • 映射图生成                                  │   │
│  │    • 逐行姿态校正 (!)                            │   │
│  └────────────────────┬────────────────────────────┘   │
│                         │                                │
│                         ▼                                │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 5. 图像重映射模块                                │   │
│  │    • 双线性插值                                  │   │
│  │    • 边界处理                                    │   │
│  │    • 输出: 校正后的视频                          │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 5.2 关键代码分析

#### 时间戳处理

```cpp
// 从 simulate.cpp

// 读取曝光数据
ins::AlignedVector_t<ins::Stabilizer::ExposureData> exposuredatas;
success = ins::io::readTxtExposureData(expo_data_path.c_str(), exposuredatas);

// 时间戳处理
double delay_time_sec = delay_time * 1e-3;  // 延迟补偿 (ms -> s)
for (auto &exposuredata : exposuredatas) {
    exposuredata.tick_ -= firsttimestamp;  // 去掉基偏移

    if (exposuredata.tick_ >= 0) {
        // 关键: 计算帧中心时刻
        // t_center = t_read - T_exposure/2 + delay
        exposuredata.tick_ = exposuredata.tick_ * to_second_factor
                           + delay_time_sec
                           - exposuredata.shutter_time_ * 0.5;

        exposuredatas_new.push_back(exposuredata);
    }
}

原理说明:
  1. shutter_time_: 曝光时长 (秒)
  2. tick_: 帧完成读出的时刻
  3. delay_time: 系统延迟 (ms)
     - 传感器延迟 (~1ms)
     - 算法延迟 (~1-2ms)
     - 手动调整用于对齐

  为什么要减去 half_exposure?
    ┌──────────────────────┐
    │  Row 0 曝光 (开始)    │
    │       ↓              │
    │  Row N 曝光 (开始)    │  T_sweep
    │       ↓              │
    │  Row 0 读出 (结束)    │
    │       ↓              │
    │  Row N 读出 (结束)    │  ← tick_ 记录的时刻
    └──────────────────────┘
             ↑
          中心时刻
      = tick_ - T_sweep/2
      ≈ tick_ - T_exposure/2 (近似)
```

#### 姿态查询

```cpp
// 逐帧查询姿态
for (int frame_idx = 0; frame_idx < exposuredatas.size(); frame_idx++) {
    double timestamp_sec = exposuredatas[frame_idx].tick_;

    // 查询中心时刻的姿态
    Quaterniond original_pose;
    imu_processor->getSmoothedQuaternion(original_pose, timestamp_sec);

    // 查询平滑后的姿态
    Quaterniond smoothed_pose;
    ret = pose_filter->getSmoothedFootageOrient(timestamp_sec, smoothed_pose);

    // 后续处理...
}

// IMU处理器内部实现 (简化)
void ImuProcessManager::getSmoothedQuaternion(
    Quaterniond& q,
    double timestamp
) {
    // 1. 找到相邻的IMU数据
    auto iter = std::upper_bound(
        gyro_buffer_.begin(),
        gyro_buffer_.end(),
        timestamp,
        [](double t, const GyroData& d) { return t < d.timestamp; }
    );

    // 2. 线性插值 (或SLERP)
    if (iter != gyro_buffer_.begin() && iter != gyro_buffer_.end()) {
        const auto& prev = *(iter - 1);
        const auto& next = *iter;

        double alpha = (timestamp - prev.timestamp) /
                       (next.timestamp - prev.timestamp);

        q = slerp(prev.q, next.q, alpha);  // 球面线性插值
    }
}
```

#### 滚动快门校正

```cpp
// FlowState系统内部 (推断实现)

void MonoFlowstateSystem::getFlowMap(
    double timestamp,
    SweepMode sweep_mode,
    Image2fRef& flow_map,
    ...
) {
    // 1. 获取中心时刻的姿态
    Quaterniond q_center;
    imu_processor_->getSmoothedQuaternion(q_center, timestamp);

    // 2. 计算扫描参数
    double sweep_time = this->sweep_time_;  // 例如 25ms
    int map_height = flow_map.rows();

    // 3. 对于映射图的每一行
    for (int y = 0; y < map_height; y++) {
        // 3.1 计算该行的相对时间
        double t_offset = (y - map_height / 2.0) / map_height * sweep_time;
        double t_row = timestamp + t_offset;

        // 3.2 获取该行曝光时刻的姿态
        Quaterniond q_row;
        imu_processor_->getSmoothedQuaternion(q_row, t_row);

        // 3.3 计算相对旋转
        Quaterniond dq = q_center * q_row.conjugate();

        // 3.4 应用到映射图的该行
        for (int x = 0; x < flow_map.cols(); x++) {
            // 计算源坐标
            Vector2f src_coord = applyRotation(
                flow_map(y, x),
                dq,
                intrinsics_
            );

            flow_map(y, x) = src_coord;
        }
    }
}

// applyRotation 函数 (简化)
Vector2f applyRotation(
    const Vector2f& pixel,
    const Quaterniond& dq,
    const Intrinsics& K
) {
    // 1. 去畸变 (如果需要)
    Vector2f undistorted = K.undistort(pixel);

    // 2. 归一化
    Vector3f ray = K.unproject(undistorted);

    // 3. 应用旋转
    Vector3f rotated = dq.toRotationMatrix() * ray;

    // 4. 重新投影
    Vector3f projected = K.project(rotated);

    // 5. 重新畸变 (如果需要)
    Vector2f redistorted = K.distort(projected.head<2>());

    return redistorted;
}
```

#### 迭代配置

```cpp
// 设置迭代次数
system->setDeRollingShutterIter(drs_iter_num);

// 参数选择:
// drs_iter_num = 0: 不校正
// drs_iter_num = 1: 单步校正 (快速)
// drs_iter_num = 3-5: 标准迭代 (平衡)
// drs_iter_num = 10+: 深度精化 (高精度)

// 迭代逻辑 (推断)
for (int iter = 0; iter < drs_iter_num; iter++) {
    // 1. 生成当前映射图
    generateFlowMap(timestamp, current_pose_estimate);

    // 2. 计算光流/特征匹配
    refineFeaturesUsingFlow();

    // 3. 更新姿态估计
    updatePoseEstimate();

    // 4. 检查收敛
    if (checkConvergence()) break;
}
```

### 5.3 性能优化

#### 粗网格映射

```
问题: 逐像素计算太慢 (4K图像 = 8M像素)

解决: 使用粗网格 + 双线性插值

原始分辨率: 3840 × 2160 (4K)
映射图分辨率: 40 × 30 (粗网格)

┌─────────────────────────────────────┐
│  粗网格 (40×30)                     │
│  ┌─┬─┬─┬─┬─┬─┬─┬─┐                │
│  │ │ │ │ │ │ │ │ │                │
│  ├─┼─┼─┼─┼─┼─┼─┼─┤                │
│  │ │ │ │ │ │ │ │ │                │
│  ├─┼─┼─┼─┼─┼─┼─┼─┤                │
│  │ │ │ │ │ │ │ │ │                │
│  └─┴─┴─┴─┴─┴─┴─┴─┘                │
│                                     │
│  每个网格单元: ~100 × 70 像素       │
│  计算量减少: ~1000×                 │
│                                     │
│  使用时: 双线性插值到全分辨率        │
└─────────────────────────────────────┘

代码:
  const int map_cols = 40;
  const int map_rows = 30;
  cv::Mat cv_map(map_rows, map_cols, CV_32FC2);

  // 只计算粗网格的映射
  system->getFlowMap(..., eig_map, ...);

  // 使用时插值
  ResizeMap<cv::Point2f>(cv_map, dst_map);  // 插值到全分辨率
```

#### SIMD优化

```
NEON指令集并行:

// 伪代码
void applyRotationSIMD(
    float* dst,
    const float* src,
    const float* rot_matrix,
    int N
) {
    // NEON寄存器: 128位 = 4×float
    float32x4_t rot0 = vld1q_f32(rot_matrix + 0);
    float32x4_t rot1 = vld1q_f32(rot_matrix + 4);
    float32x4_t rot2 = vld1q_f32(rot_matrix + 8);

    for (int i = 0; i < N; i += 4) {
        // 加载4个像素
        float32x4_t x = vld1q_f32(src + i * 2 + 0);
        float32x4_t y = vld1q_f32(src + i * 2 + 1);

        // 并行应用旋转
        float32x4_t dst_x = vfmaq_f32(
            vmulq_f32(rot0, x),
            rot1, y
        );
        float32x4_t dst_y = vfmaq_f32(
            vmulq_f32(rot2, x),
            rot3, y
        );

        // 存储
        vst1q_f32(dst + i * 2 + 0, dst_x);
        vst1q_f32(dst + i * 2 + 1, dst_y);
    }
}

加速比: ~3-4×
```

#### 缓存优化

```
姿态缓存策略:

问题: 每帧查询多次姿态 (每行一次)
      直接插值太慢

解决: 预计算并缓存

// 预计算姿态缓存
std::vector<Quaterniond> pose_cache;
pose_cache.resize(map_rows);

for (int y = 0; y < map_rows; y++) {
    double t_row = timestamp +
        (y - map_rows/2.0) / map_rows * sweep_time;

    imu_processor->getSmoothedQuaternion(
        pose_cache[y],
        t_row
    );
}

// 使用时直接查表
Quaterniond q_row = pose_cache[y];

优势:
  • 避免重复插值
  • 内存访问模式友好
  • 加速比: ~10×
```

---

## 6. 参数配置与优化

### 6.1 关键参数详解

#### sweep_time (扫描时间)

```
定义: 整个图像扫描所需时间 (毫秒)

物理意义:
  • 第0行到最后一行的时间跨度
  • 决定了卷帘快门效应的强度

典型值:
  • 30 fps 1080p: ~15-20 ms
  • 30 fps 4K:    ~25-30 ms
  • 60 fps 1080p: ~15-20 ms
  • 120 fps 1080p: ~10-15 ms

设置方法:

  方法1: 从相机规格获取
    手册或datasheet中的"readout time"

  方法2: 实验测量
    a. 快速旋转相机 (已知角速度)
    b. 拍摄包含垂直直线的场景
    c. 测量图像中线条倾斜角度
    d. 计算: sweep = angle / angular_velocity

  方法3: 调优
    尝试不同值,选择视觉效果最好的

代码:
  pose_filter->setSweepTime(sweep_time * 0.001);  // ms -> s
```

#### delay_time (延迟补偿)

```
定义: IMU数据与视频帧的时间偏移 (毫秒)

组成:
  delay = sensor_delay + algorithm_delay + custom_offset

  • sensor_delay: 传感器读取延迟 (~1ms)
  • algorithm_delay: 滤波算法延迟
    - VQF: ~1ms
    - AKF: ~1-2ms
    - UKF: ~2-3ms
  • custom_offset: 手动调整

优化方法:

  1. 拍摄测试视频
     - 快速平移相机
     - 包含明显特征

  2. 调整delay_time
     - 在仿真程序中实时调整
     - 观察校正效果

  3. 选择最优值
     - 畸变最小的延迟
     - 通常在 -50 到 +50 ms 之间

代码:
  // 可视化调整
  cv::createTrackbar("delay_time_bias", "dst",
                     &delay_time_bias, 1000, NULL);

  double delay_time = base_delay + delay_time_bias * 0.1;
```

#### drs_iter_num (迭代次数)

```
定义: 滚动快门校正的迭代次数

权衡:

  迭代次数少 (0-1):
    ✓ 速度快
    ✓ 延迟低
    ✗ 校正不完整
    ✗ 残留畸变

  迭代次数中等 (3-5):
    ✓ 速度和质量的平衡
    ✓ 适用于大多数场景
    ✗ 偶尔残留小畸变

  迭代次数多 (8-10):
    ✓ 校正最完整
    ✓ 处理复杂运动
    ✗ 计算量大
    ✗ 延迟高

推荐配置:

  实时处理 (30fps+):
    drs_iter = 1-3

  离线处理:
    drs_iter = 5-10

  快速运动:
    drs_iter = 5+

  平静场景:
    drs_iter = 1-2

代码:
  system->setDeRollingShutterIter(drs_iter_num);
```

### 6.2 不同场景的配置

```
┌────────────────────────────────────────────────────────────┐
│                    场景配置指南                              │
├────────────┬───────────────┬──────────┬──────────────────┤
│ 场景       │ 特征          │ sweep    │ drs_iter         │
├────────────┼───────────────┼──────────┼──────────────────┤
│ 静止/三脚架│ 几乎无运动    │ 默认值   │ 0-1 (可禁用)      │
│            │               │          │                  │
│ 平慢移动   │ 低速平移      │ 默认值   │ 2-3              │
│            │               │          │                  │
│ 正常行走   │ 中等抖动      │ 默认值   │ 3-5              │
│            │               │          │                  │
│ 跑步       │ 快速抖动      │ +10-20%  │ 5-8              │
│            │               │ (估计)   │                  │
│ 第一人称   │ 高频抖动      │ 默认值   │ 3-5              │
│            │               │          │                  │
│ 无人机     │ 平滑但有风    │ 默认值   │ 3-5              │
│            │               │          │                  │
│ 车载       │ 振动+旋转     │ +20%     │ 5-8              │
└────────────┴───────────────┴──────────┴──────────────────┘
```

### 6.3 调优工作流

```
卷帘快门校正调优流程:

1. 基础配置
   ├─ 从相机规格获取 sweep_time
   ├─ 设置默认 delay_time = 0
   └─ 设置 drs_iter = 3

2. 拍摄测试素材
   ├─ 场景: 包含垂直线条 (建筑、门窗)
   ├─ 运动: 快速平移 + 旋转
   └─ 光照: 室内外各拍一段

3. 调整延迟
   ├─ 运行仿真程序
   ├─ 调整 delay_time_bias
   ├─ 观察线条是否变直
   └─ 记录最优值

4. 微调 sweep_time
   ├─ 如果仍有倾斜: 增加 sweep_time
   ├─ 如果过度校正: 减少 sweep_time
   └─ 步长: 1-2 ms

5. 调整迭代次数
   ├─ 从 drs_iter = 1 开始
   ├─ 逐步增加直到视觉改善
   ├─ 平衡质量和速度
   └─ 实时处理通常不超过5

6. 验证
   ├─ 多个场景测试
   ├─ 不同光照条件
   └─ 记录最优配置组合
```

---

## 7. 高级优化技术

### 7.1 自适应扫描时间

```
问题: 扫描时间不是常数

原因:
  • 不同曝光模式
  • 不同分辨率
  • 不同读出模式

解决: 动态估算

算法:

  1. 使用光流估算局部运动
     flow_x(y) = mean(dI/dx) for row y

  2. 计算运动梯度
     d_flow/dy = (flow_x(y+1) - flow_x(y-1)) / 2

  3. 估算扫描时间
     sweep_estimated = flow_gradient / angular_velocity

  4. 平滑更新
     sweep_adaptive = α·sweep_estimated + (1-α)·sweep_previous

实现:
  if (adaptive_sweep_enabled) {
      double estimated = estimateSweepFromFlow(current_frame);
      double alpha = 0.1;  // 平滑因子
      sweep_time = alpha * estimated + (1 - alpha) * sweep_time;
  }
```

### 7.2 局部校正

```
问题: 全局统一校正可能不精确

原因:
  • 场景中不同深度
  • 运动不均匀
  • 遮挡边界

解决: 分块校正

方法:

  1. 将图像分成网格
     ┌───┬───┬───┬───┐
     │   │   │   │   │
     ├───┼───┼───┼───┤
     │   │   │   │   │
     ├───┼───┼───┼───┤
     │   │   │   │   │
     └───┴───┴───┴───┘

  2. 每个块独立估计运动
  3. 应用局部校正
  4. 边界融合

优势:
  • 处理复杂运动
  • 提高精度

劣势:
  • 计算量大
  • 可能不连续
```

### 7.3 深度感知校正

```
问题: 旋转校正假设无限深度

现实: 不同深度需要不同校正

原理:

  p_image = f · tan(θ)
  Δp = f · (tan(θ + Δθ) - tan(θ))
     ≈ f · Δθ / cos²(θ)  (小角度)

  深度相关的平移:
  对于距离 d 的物体:
    Δp_translation = f · v·Δt / d

方法:

  1. 估计深度图
     • 双目立体
     • 结构从运动
     • 单目深度估计

  2. 深度相关校正
     for each pixel p:
       d = depth_map[p]
       correction = f * v * sweep_time / d
       p_corrected = p + correction

  3. 融合旋转和平移校正

Insta360可能未实现:
  • 计算量大
  • 需要深度信息
  • 收益有限 (通常旋转占主导)
```

### 7.4 机器学习方法

```
前沿研究方向: 用神经网络学习校正

网络结构:

  输入: 畸变图像 + IMU数据
        ↓
  [特征提取器]
    CNN提取空间特征
    LSTM处理时间序列
        ↓
  [姿态估计网络]
    预测逐行姿态
        ↓
  [校正层]
    可微的逆向变换
        ↓
  输出: 校正图像

训练:
  损失函数:
    L = ||I_pred - I_gt||²
      + λ·||q_pred - q_gt||²
      + smoothness_regularization

  数据:
    • 合成数据 (已知运动)
    • 实际数据 (全局快门参考)
    • 自监督 (光流一致性)

优势:
  • 端到端优化
  • 学习复杂模式
  • 可能超越传统方法

挑战:
  • 需要大量数据
  • 实时性要求
  • 泛化能力
```

---

## 8. 总结

### 8.1 核心要点

```
卷帘快门校正的关键:

1. 理解物理原理
   • 逐行曝光导致时间差
   • 相机运动造成畸变
   • 扫描时间决定影响程度

2. 精确时间同步
   • 帧中心时刻 = 读出时刻 - 曝光/2 - 延迟
   • 行时刻 = 中心 + (y - H/2)/H × 扫描时间
   • IMU数据必须精确对齐

3. 姿态插值
   • 高频IMU数据
   • SLERP插值
   • 平滑滤波

4. 逆向映射
   • 从目标像素找源位置
   • 逐行应用姿态校正
   • 双线性插值

5. 迭代精化
   • 单步校正不够
   • 迭代提高精度
   • 收敛检查

6. 性能优化
   • 粗网格减少计算
   • SIMD并行加速
   • 缓存姿态查询
```

### 8.2 实践建议

```
开发防抖系统时:

1. 从简单开始
   • 先实现全局快门校正
   • 添加固定延迟补偿
   • 单步迭代

2. 逐步优化
   • 添加滚动快门校正
   • 迭代精化
   • 性能优化

3. 充分测试
   • 各种运动模式
   • 不同场景
   • 边界情况

4. 参数调优
   • 扫描时间
   • 延迟补偿
   • 迭代次数

5. 用户反馈
   • 可视化工具
   • 实时调参
   • A/B测试
```

### 8.3 未来方向

```
卷帘快门校正的发展:

1. 硬件改进
   • 更快的读出速度
   • 全局快门降低成本
   • 堆叠式传感器

2. 算法改进
   • 深度学习融合
   • 多传感器融合
   • 自适应参数

3. 应用扩展
   • 高帧率视频
   • 8K+分辨率
   • VR/AR应用

4. 实时优化
   • GPU加速
   • 专用硬件
   • 边缘计算
```

---

*本文档深入解析了卷帘快门校正的物理原理、数学模型、算法实现和优化方法。结合 Insta360 的实际代码，提供了从理论到实践的完整视角。*
